# fcnCreateInflightTripSets

## Metadata
- **Tipo**: SRL Function
- **Retorna**: void
- **Ubicación**: `CrewRulesRepository\TechnicalLibrary\Pay\InflightPay\Functions\fcnCreateInflightTripSets`

## Propósito
US5674 - Akshay M - 3/13/2015 - Creating Inflight specific tripSet logic

## Parámetros

| Nombre | Tipo | Descripción |
|--------|------|-------------|
| aPayCrewMemberLine | PayCrewMemberLine | |
| aCrewPayResponse | CrewPayResponse | |

## Lógica de negocio

```blaze
aPayTrip is some PayTrip initially null.aTripSet is some TripSet initially null.nextPayTrip is some PayTrip initially null.tripListIndex is an integer initially 0.tripSetIndex is an integer.counter is an integer.prevPayTrip is some PayTrip initially null.removeTripSets is an array of string. //DE7313if (aPayCrewMemberLine.tripList <> null and aPayCrewMemberLine.tripList.size() > 0) then {// WE WILL BE WORKING TRU THE LIST IN REVERSE (FROM LAST ELEMENT TO FIRST)...tripListIndex = aPayCrewMemberLine.tripList.size() - 1.    while (tripListIndex >= 0) do {                    aPayTrip = aPayCrewMemberLine.tripList.get(tripListIndex).fcnShow("================>>> fcnCreateInflightTripSets IN LOOP PayTrip: " aPayTrip.tripName " with index " tripListIndex).if ((aPayTrip.assignmentLabel = (ignoring case) "I") or // NO INFLIGHT I-LABEL TRIPS ALLOWED IN TRIP SETS...(fcnIsReserveBlock(aPayTrip) is equal to true) or  // NO RESERVE BLOCKS ALLOWED IN TRIP SETS...(fcnIsNonFlyTrip(aPayTrip) is equal to true) or    // NO NON-FLY TRIP ALLOWED IN TRIP SETS...aPayTrip.nonDeadheadBeginDateTime= null) then      // ALL LEGS IN THE TRIP ARE RS DEADHEADS{fcnShow("===>>> excluding trip " aPayTrip.tripName " from inclusion in a Tripset because it is either a nonfly, a reserve block or an Inflight I-Label trip...").}else //  IS A FLYING TRIP{ if fcnIsPayTripMultiDutyPeriod(aPayTrip) then // SET EVERY TRIP'S multiDutyPeriodTrip property...{ aPayTrip.multiDutyPeriodTrip = true.aPayTrip.tripPay.multiDutyPeriodTrip = true.fcnShow("===>>  fcnCreateInflightTripSets" aPayTrip.tripName " is a multi Duty Period Trip!!!" ).}aTripSet = null.if (aPayCrewMemberLine.tripSetList.size() = 0) then // no trip sets currently exist, so create a tripset and assign the Paytrip to it{ aTripSet = fcnCreateTripSet(aPayTrip,true,aCrewPayResponse.schedulePeriodPayList).aPayCrewMemberLine.addTripSet(aTripSet).fcnShow("================>>> fcnCreateInflightTripSets adding PayTrip: " aPayTrip.tripName " to newly created TripSet: " aTripSet.tripSetName).}else if (nextPayTrip is not equal to null) then    // at least one trip set exists and netPayTrip is not null{if (nextPayTrip.nonDeadheadBeginDateTime <> null and aPayTrip.nonDeadheadEndDateTime <> null and aPayTrip.nonDeadheadEndDateTime.plusHours(8) > nextPayTrip.nonDeadheadBeginDateTime) then {                   if (aPayTrip.multiDutyPeriodTrip) then // assign PayTrip to same tripset as prevPayTrip{fcnShow("Current Pay Trip has multiple duty periods...").aTripSet = a TripSet.aTripSet.tripSetName = "TripSet-" fcnGetSchedulePeriodNameForDateTime(aPayTrip.nonDeadheadBeginDateTime,aCrewPayResponse.schedulePeriodPayList) "-" aPayTrip.nonDeadheadBeginDateTime.toString().//--------------------> DE5957aTripSet.schedulePeriodName =  fcnGetSchedulePeriodNameForDateTime(aPayTrip.nonDeadheadBeginDateTime,aCrewPayResponse.schedulePeriodPayList).//--------------------> DE5957aTripSet.startDateTime = aPayTrip.nonDeadheadBeginDateTime.aTripSet.endDateTime = aPayTrip.nonDeadheadEndDateTime.aTripSet.incrementCountOfMultiDutyPeriodTrips(). aTripSet.addPayTrip(aPayTrip).aPayTrip.tripSet = aTripSet.tripSetIndex = tripListIndex + 1.prevPayTrip = aPayTrip.removeTripSets.clear(). //DE7313// loop through PayTrips while (tripSetIndex < aPayCrewMemberLine.tripList.size()) do {if (prevPayTrip.nonDeadheadEndDateTime <> null and aPayCrewMemberLine.tripList.get(tripSetIndex).nonDeadheadBeginDateTime <> null andprevPayTrip.nonDeadheadEndDateTime.plusHours(8) > aPayCrewMemberLine.tripList.get(tripSetIndex).nonDeadheadBeginDateTime) then {if((fcnIsReserveBlock(aPayCrewMemberLine.tripList.get(tripSetIndex)) is equal to false) and  // NO RESERVE BLOCKS ALLOWED IN TRIP SETS...   (fcnIsNonFlyTrip(aPayCrewMemberLine.tripList.get(tripSetIndex)) is equal to false)) then //and    //and aPayCrewMemberLine.tripList.get(tripSetIndex).tripSet.countOfMultiDutyPeriodTrips > 0) then // or prevPayTrip.tripSet.countOfMultiDutyPeriodTrips > 0)) then  // NO NON-FLY TRIP ALLOWED IN TRIP SETS...{fcnShow("===>>> fcnCreateInflightTripSets PayTrip " aPayCrewMemberLine.tripList.get(tripSetIndex).tripName " belongs to the new TripSet.").// Instead of just maintaining a count of trip sets to delete, keeping track of the specific names to delete.if( aPayCrewMemberLine.tripList.get(tripSetIndex).tripSet <> null) then {removeTripSets.append(aPayCrewMemberLine.tripList.get(tripSetIndex).tripSet.tripSetName).}aTripSet.endDateTime = prevPayTrip.nonDeadheadEndDateTime.aPayCrewMemberLine.tripList.get(tripSetIndex).tripSet = aTripSet.aTripSet.addPayTrip(aPayCrewMemberLine.tripList.get(tripSetIndex)).if (aPayCrewMemberLine.tripList.get(tripSetIndex).multiDutyPeriodTrip) thenaTripSet.incrementCountOfMultiDutyPeriodTrips(). prevPayTrip = aPayCrewMemberLine.tripList.get(tripSetIndex).}elsefcnShow("===>>> excluding trip " aPayTrip.tripName " from inclusion in a Tripset because it is either a nonfly, a reserve block or an Inflight I-Label trip...").} tripSetIndex += 1.}counter = 0.while (counter < removeTripSets.count and 0 < aPayCrewMemberLine.tripSetList.size()) do // Delete empty PayTrips{// Only delete trip sets whose names are in the list to deleteif (removeTripSets.contains(aPayCrewMemberLine.tripSetList.get(aPayCrewMemberLine.tripSetList.size() - 1).tripSetName)) then {fcnShow("===>>> fcnCreateInflightTripSets Destroying TripSet : " aPayCrewMemberLine.tripSetList.get(aPayCrewMemberLine.tripSetList.size() - 1).tripSetName).aPayCrewMemberLine.tripSetList.set((aPayCrewMemberLine.tripSetList.size() - 1), null).aPayCrewMemberLine.tripSetList.remove(aPayCrewMemberLine.tripSetList.size() - 1).}counter += 1.}aPayCrewMemberLine.addTripSet(aTripSet).fcnShow("===>>> fcnCreateInflightTripSets   Next trip contains no multi-DutyPeriod trip Just added TripSet " aTripSet.tripSetName " starts at " aTripSet.startDateTime ", ends at " aTripSet.endDateTime).fcnShow("================>>> fcnCreateInflightTripSets adding PayTrip: " aPayTrip.tripName " to newly created TripSet: " aTripSet.tripSetName).}// PayTrip is NOT a Multi-DutyPeriod tripelse{fcnShow("Current Pay Trip does NOT have multiple duty periods...").//  TripSet has at least 1 multi-DutyPeriod trip, add to existing trip setif (nextPayTrip.tripSet is not equal to null and nextPayTrip.tripSet.countOfMultiDutyPeriodTrips > 0) then   {// assign to same tripset as nextPayTripaTripSet = nextPayTrip.tripSet.aTripSet.startDateTime = aPayTrip.nonDeadheadBeginDateTime.    // extend start time of trip set to reflect addition of new PayTripaTripSet.addPayTrip(aPayTrip).aPayTrip.tripSet = aTripSet.if (aPayTrip.multiDutyPeriodTrip) thenaTripSet.incrementCountOfMultiDutyPeriodTrips(). fcnShow("================>> fcnCreateInflightTripSets adding PayTrip " aPayTrip.tripName " to pre-existing TripSet: " aTripSet.tripSetName " where the next duty period has multiple duty periods").} // TripSet has NO multi-DutyPeriod trips so assign aPayTrip to a new trip setelse{aTripSet = fcnCreateTripSet(aPayTrip,true,aCrewPayResponse.schedulePeriodPayList).aPayCrewMemberLine.addTripSet(aTripSet).fcnShow("===>>> fcnCreateInflightTripSets Just added TripSet " aTripSet.tripSetName " starts at " aTripSet.startDateTime ", ends at " aTripSet.endDateTime).fcnShow("================>>> fcnCreateInflightTripSets adding PayTrip: " aPayTrip.tripName " to newly created TripSet: " aTripSet.tripSetName).}}}// NOT less than 8 hours from next trip...so create a new trip set and assign aPayTrip to itelse {  aTripSet = fcnCreateTripSet(aPayTrip,true,aCrewPayResponse.schedulePeriodPayList).aPayCrewMemberLine.addTripSet(aTripSet).fcnShow(",,,,,,,,,fcnCreateInflightTripSets  " aPayTrip.tripName " is NOT < 8 hours from next trip").fcnShow("===>>> fcnCreateInflightTripSets Just added Trip set " aTripSet.tripSetName " starts at " aTripSet.startDateTime ", ends at " aTripSet.endDateTime).fcnShow("================>>> fcnCreateInflightTripSets adding PayTrip: " aPayTrip.tripName " to newly created TripSet: " aTripSet.tripSetName).}}nextPayTrip = aPayTrip.  // at this point, nextpayTrip represents the last PayTrip which was assigned to a TripSetfcnShow("..................fcnCreateInflightTripSets  nextPayTrip now =  " aPayTrip.tripName).}tripListIndex -= 1.}// Reverse list so TripsSets are in chronological orderaPayCrewMemberLine.reverseTripSetList().// Enforce the start and end times of the TripSet to make certain that Tripset start is earliest start of // all PayTrips in the Tripset  and TripSet end is latest end of all PayTrips in the TripSet...if (aPayCrewMemberLine.tripSetList <> null and aPayCrewMemberLine.tripSetList.size() > 0) then {for each TripSet in aPayCrewMemberLine.tripSetList as an array of TripSet do {aTripSet = it.if (aTripSet.payTripList <> null and aTripSet.payTripList.size() > 0) then {for each PayTrip in aTripSet.payTripList as an array of PayTrip do{if (it.nonDeadheadBeginDateTime <> null and it.nonDeadheadBeginDateTime.isBefore(aTripSet.startDateTime)) then aTripSet.startDateTime = it.nonDeadheadBeginDateTime.if (it.nonDeadheadEndDateTime <> null and it.nonDeadheadEndDateTime.isAfter(aTripSet.endDateTime)) then aTripSet.endDateTime = it.nonDeadheadEndDateTime.                                                                                // XERF trip set to schedule period pay...if (it.tripPay <> null and it.tripPay.startSchedulePeriodPay <> null and it.firstFlyingLeg <> null) then{ schedulePeriodName is a string initially fcnGetSchedulePeriodNameForDateTime(it.firstFlyingLeg.scheduledDepartureDateTime,aCrewPayResponse.schedulePeriodPayList);if (aTripSet.schedulePeriodPayList.contains(aCrewPayResponse.getSchedulePeriodPayByName(schedulePeriodName)) = false) thenaTripSet.schedulePeriodPayList.add(aCrewPayResponse.getSchedulePeriodPayByName(schedulePeriodName)).// add to the TripSet.SchedulePeriodPayListif (aCrewPayResponse.getSchedulePeriodPayByName(fcnGetSchedulePeriodNameForDateTime(it.firstFlyingLeg.scheduledDepartureDateTime,aCrewPayResponse.schedulePeriodPayList)).tripSetList.contains(aTripSet) = false) thenaCrewPayResponse.getSchedulePeriodPayByName(fcnGetSchedulePeriodNameForDateTime(it.firstFlyingLeg.scheduledDepartureDateTime,aCrewPayResponse.schedulePeriodPayList)).tripSetList.add(aTripSet).// add to the SchedulePeriodPay.TripSetList}}}              }}}// Log the TripSet information if in debug modeif (debugMode) then {for each TripSet in aPayCrewMemberLine.tripSetList as an array of TripSet do {fcnShow("===>>> TripSet " it.tripSetName).fcnShow("                ===>>> starts at " DateTimeUtilities.getShortDateTimeString(it.startDateTime)).fcnShow("                ===>>> ends at " DateTimeUtilities.getShortDateTimeString(it.endDateTime)).fcnShow("                ===>>> number of trips with multiple duty periods " it.countOfMultiDutyPeriodTrips).for each PayTrip in it.payTripList as an array of PayTrip do {fcnShow("                ===>>> and contains PayTrips: ").fcnShow("                      ...TripName: " it.tripName " start: " DateTimeUtilities.getShortDateTimeString(it.nonDeadheadBeginDateTime) " end: " DateTimeUtilities.getShortDateTimeString(it.nonDeadheadEndDateTime)).}}}fcnShow("===>>> " aPayCrewMemberLine.tripSetList.size() " TripSets were created...").
```

## Dependencias

Esta function llama a:

- [fcn](fcn.md)
- [fcnCreateTripSet](fcnCreateTripSet.md)
- [fcnGetSchedulePeriodNameForDateTime](fcnGetSchedulePeriodNameForDateTime.md)
- [fcnIsNonFlyTrip](fcnIsNonFlyTrip.md)
- [fcnIsPayTripMultiDutyPeriod](fcnIsPayTripMultiDutyPeriod.md)
- [fcnIsReserveBlock](fcnIsReserveBlock.md)
- `fcnShow()`
- [main](main.md)
- [ping](ping.md)

## Historial de cambios

```
US5674 - Akshay M - 3/13/2015 - Creating Inflight specific tripSet logic
8/30/2015 - Melissa S - DE7313 - Fixed logic for deleting empty trip sets
it.tripPay.startSchedulePeriodPay
```


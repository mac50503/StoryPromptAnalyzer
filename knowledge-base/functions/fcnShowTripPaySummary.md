# fcnShowTripPaySummary

## Metadata
- **Tipo**: SRL Function
- **Retorna**: void
- **Ubicación**: `CrewRulesRepository\TechnicalLibrary\Pay\CommonPay\Functions\fcnShowTripPaySummary`

## Propósito
US18085 - Melissa S - 7/29/2014 - Refactored for performance  - Added check for debugMode to avoid looping if not in debugMode

## Parámetros

| Nombre | Tipo | Descripción |
|--------|------|-------------|
| aTripPay | TripPay | |
| aTripPayInflight | TripPayInflight | |

## Lógica de negocio

```blaze
if (debugMode) then {isInflight is a boolean initially false.if (aTripPayInflight <> null ) thenisInflight = true.rapAssociationSeqNum is a string initially "none".reportsOnHoliday is a string initially "no".if (fcnIsReserveBlock(aTripPay.payTrip)) then{associatedReserveTrip is a string initially "null".if (aTripPay.payTrip.associatedReserveTrip <> null) thenassociatedReserveTrip = aTripPay.payTrip.associatedReserveTrip.tripName.fcnShow("==>Reserve Block Trip: " aTripPay.tripNameAndDate " start = " DateTimeUtilities.getShortDateTimeString(aTripPay.payTrip.beginDateTime)  " end = " DateTimeUtilities.getShortDateTimeString(aTripPay.payTrip.endDateTime) ", seq# = " aTripPay.sequenceNumber " ...associatedReserveTrip = " associatedReserveTrip).   }else{tripStr is a string initially "Trip".if (isInflight = false and fcnIsReserveTrip(aTripPay.payTrip)) thentripStr = "Reserve Trip".if (isInflight = true and aTripPayInflight.isReserveTrip) thentripStr = "Reserve Trip".fcnShow("==>" tripStr ": " aTripPay.tripNameAndDate " start = " DateTimeUtilities.getShortDateTimeString(aTripPay.payTrip.beginDateTime)  " end = " DateTimeUtilities.getShortDateTimeString(aTripPay.payTrip.endDateTime) ", seq# = " aTripPay.sequenceNumber).   }fcnShow(".........label = " aTripPay.assignmentLabel " ...class = " aTripPay.tripClass " ...trip type = " aTripPay.tripType " ...passed credit type = " aTripPay.payTrip.creditType " ...credit type = " aTripPay.creditType " ...nonfly code = " aTripPay.nonFlyCode " ...TAFB = " aTripPay.timeAwayFromBase " ...starts in SP = " aTripPay.startsInSchedulePeriod  " ...ends in SP = " aTripPay.endsInSchedulePeriod).fcnShow(".........basePay = " fcnRoundUpAt2DecimalPlaces(aTripPay.payTrip.basePay)  " ...payValue = " fcnRoundUpAt2DecimalPlaces(aTripPay.payValue)  " ...thisMonthPay = " fcnRoundUpAt2DecimalPlaces(aTripPay.thisMonthPay) " ...lastMonthPay = " fcnRoundUpAt2DecimalPlaces(aTripPay.lastMonthPay) "...nextMonthPay = " fcnRoundUpAt2DecimalPlaces(aTripPay.nextMonthPay)  " ...conusPay = " fcnRoundUpAt2DecimalPlaces(aTripPay.conusPay)  " ...oconusPay = " fcnRoundUpAt2DecimalPlaces(aTripPay.oconusPay)).  fcnShow(".........pay no premium = " fcnRoundUpAt2DecimalPlaces(aTripPay.payValueNoPremiumLegs) " ...holidayPremium = " fcnRoundUpAt2DecimalPlaces(aTripPay.holidayPremium) " ...perdiemPay = " aTripPay.perdiemPay  " ...mdhPayValue = " aTripPay.mdhPayValue  " ...amReserveCredit = " aTripPay.amReserveCredit  " ...crew position = " aTripPay.payTrip.assignmentCrewPosition).   if (aTripPay.payTrip.tripSet <> null) then{fcnShow(".........conus day count = " aTripPay.payTrip.tripSet.numConusDay " ...oconus day count  = " aTripPay.payTrip.tripSet.numOconusDay " ...total per diem non tax limit = " aTripPay.payTrip.tripSet.perdiemLimit "...conus non tax limit = " aTripPay.payTrip.tripSet.conusLimit " ...oconus non tax limit = " aTripPay.payTrip.tripSet.oconusLimit).}if (isInflight) then{fcnShow(".........reserve trip? = " aTripPayInflight.isReserveTrip " ...thr = " aTripPayInflight.thrValue  " ...thr to start of RON Rig = " aTripPayInflight.thrToStartOfRonRig " ...adg = " aTripPay.tripPayInflight.adgValue " ...sum of duties (base) = " fcnRoundUpAt2DecimalPlaces(aTripPayInflight.baseDutyPeriodSum) " ...sum of duties = " aTripPayInflight.dutyPeriodSum " ...positionA = " aTripPayInflight.positionA).fcnShow(".........tripRig = " aTripPayInflight.tripRIG " ...RON RIG total = " aTripPayInflight.ronRigTotal  " ...RON premium = " aTripPayInflight.ronPremium  " ...duty premium total = " aTripPayInflight.dutyPremiumTotal " ...associated airport standby = " aTripPay.payTrip.associatedAirportStandby " ... distributed trip pay = " fcnRoundUpAt2DecimalPlaces(aTripPay.distributedPay)).fcnShow(".........this month leg premium = " fcnRoundUpAt2DecimalPlaces(aTripPayInflight.thisMonthLegPremium) " ... nonRONdutyPeriodBasePay = " fcnRoundUpAt2DecimalPlaces(aTripPayInflight.nonRONdutyPeriodBasePay) " ...this month Non-RON leg premium = " fcnGetNonRONpremium(aTripPay)).fcnShow(".........sum of leg pay for trip = "  aTripPayInflight.legPayForTrip "...sum of leg base pay for trip = "  fcnGetSumOfLegBasePayForTrip(aTripPay.payTrip) " ...highestTripRig = " aTripPayInflight.highestTripRig " ...highestDutyRigSum = " aTripPayInflight.highestDutyRigSum " ...rigsGreaterThanPremium = " aTripPayInflight.rigsGreaterThanPremium " ...leg premium = " aTripPayInflight.legPremium). fcnShow(".........sum of non RON leg premium = "  aTripPayInflight.nonRONlegPremium "...sum of non RON leg premium over 1.5 = "aTripPayInflight.nonRONlegPremiumOverJV).}elsefcnShow(".........thr = " aTripPay.tripHourRatio " ...adg = " aTripPay.averageDailyGuaranty " ...sum of duties = " aTripPay.sumDutyPeriodCredits " ...premiumPayValue = " aTripPay.premiumPayValue).   if (aTripPay.dutyPeriodPayList <> null and aTripPay.dutyPeriodPayList.size() > 0) then{for each DutyPeriodPay in aTripPay.dutyPeriodPayList as an array of DutyPeriodPay do{if (isInflight) thenrapAssociationSeqNum = fcnGetRapAssociationListAsString(it.payDutyPeriod).else if (it.payDutyPeriod.rapAssociation <> null) then rapAssociationSeqNum = it.payDutyPeriod.rapAssociation.sequenceNumber as a string.if (it.payDutyPeriod.reportsOnHoliday) thenreportsOnHoliday = "yes".fcnShow(".........Duty Period: " it.sequenceNumber " " it.payDutyPeriod.beginLocation "-" it.payDutyPeriod.endLocation " ...skd report = "  DateTimeUtilities.getShortDateTimeString(it.scheduledReportDateTime) " ...report = " DateTimeUtilities.getShortDateTimeString(it.reportDateTime)   " ...skd release = "  DateTimeUtilities.getShortDateTimeString(it.scheduledReleaseDateTime) " ...release = " DateTimeUtilities.getShortDateTimeString(it.releaseDateTime)).fcnShow("............reportsOnHoliday = " reportsOnHoliday " ...rapAssociation = " rapAssociationSeqNum " ...passed in credit type = " it.payDutyPeriod.creditType  " ...response credit type = " it.creditType " ...duty type = " it.payDutyPeriod.dutyType).fcnShow("............basePay = " it.basePay " ...payValue = " fcnRoundUpAt2DecimalPlaces(it.payValue)  " ...premiumPayValue = " fcnRoundUpAt2DecimalPlaces(it.premiumPayValue) " ...thisMonthPay = " fcnRoundUpAt2DecimalPlaces(it.thisMonthPay)  " ...lastMonthPay = " fcnRoundUpAt2DecimalPlaces(it.lastMonthPay)  " ...nextMonthPay = " fcnRoundUpAt2DecimalPlaces(it.nextMonthPay)).fcnShow("............pay no premium = " fcnRoundUpAt2DecimalPlaces(it.payValueNoPremiumLegs)  " ...holidayPremium = " it.holidayPremium  " ...mdhPayValue = " it.mdhPayValue  " ...duty trip excess = " it.tripExcess  " ...positionA = " it.positionA " ...dpFlags = " it.payDutyPeriod.dpFlags).fcnShow("............dpm = " it.dutyPeriodMinimum " ...dhr = " it.dutyHourRatio " ...sumLegBaseCredits = " fcnRoundUpAt2DecimalPlaces(it.sumLegBaseCredits) " ...sumLegTotalCredits = " fcnRoundUpAt2DecimalPlaces(it.sumLegTotalCredits)  " ...dutyAmount = " it.dutyAmount " ...rest after duty = " it.restAfterDuty).fcnShow("............highestDutyPeriodRig = " it.highestDutyPeriodRig " ...rigs greater than premium = " it.rigsGreaterThanPremium).if (isInflight) then{fcnShow("............proratedPay = " fcnRoundUpAt2DecimalPlaces(it.proratedPay) " ...remainingSplitGuaranty = " it.remainingSplitGuaranty " ...sumLegPremiumCredits = " it.sumLegPremiumCredits " ...distributedBasePay = " fcnRoundUpAt2DecimalPlaces(it.distributedBasePay) " ...distributedPay = " fcnRoundUpAt2DecimalPlaces(it.distributedPay)).fcnShow("............position A leg base pay = " fcnGetSumOfLegPay(it, true, true) " ...position A leg calculated pay = " fcnGetSumOfLegPay(it, false, true)).fcnShow("............not position A leg base pay = " fcnGetSumOfLegPay(it, true, false) " ...not position A leg calculated pay = " fcnGetSumOfLegPay(it, false, false)).fcnShow("............duty period RIG = " it.dutyPeriodRig " ...original skd report = " it.payDutyPeriod.payDutyPeriodInflight.originalScheduledReportDateTime " ...original skd release = " it.payDutyPeriod.payDutyPeriodInflight.originalScheduledReleaseDateTime).fcnShow("............variable report minutes = " it.payDutyPeriod.variableReportMinutes " ...variable release minutes = " it.payDutyPeriod.variableReleaseMinutes).}if (it.legPayList <> null and it.legPayList.size() > 0) then{for each LegPay in it.legPayList as an array of LegPay do{if (it.payLeg <> null) then{fcnShow("............Leg: " it.sequenceNumber  " " it.payLeg.departureLocation "-" it.payLeg.arrivalLocation " ...nonFlyCode = "  it.payLeg.nonFlyCode  " ...deadHeadCode = "  it.payLeg.deadHeadCode " ...credit type = " it.creditType " ...Premium Pay Code = " it.premiumPayCode " ...distance = " it.payLeg.distance).   fcnShow("...............skd dep time = " DateTimeUtilities.getShortDateTimeString(it.payLeg.scheduledDepartureDateTime) " ...actual dep time = " DateTimeUtilities.getShortDateTimeString(it.payLeg.actualOutDateTime)).fcnShow("...............skd arr time = " DateTimeUtilities.getShortDateTimeString(it.payLeg.scheduledArrivalDateTime)  " ...actual arr time = " DateTimeUtilities.getShortDateTimeString(it.payLeg.actualInDateTime)).   fcnShow("...............basePay = " it.payLeg.basePay " ...payValue = " fcnRoundUpAt2DecimalPlaces(it.payValue)  " ...leg premium = " fcnRoundUpAt2DecimalPlaces(it.payValue - it.basePay)).if (isInflight) thenfcnShow("...............crew position  = " it.payLeg.payLegInflight.crewPosition  " ...positionA = " it.positionA " ...original base pay = " it.payLeg.payLegInflight.originalBasePay).                                                                                        if (it.payLeg.legWorkCodeList <> null and it.payLeg.legWorkCodeList.size() > 0) then{for each string in it.payLeg.legWorkCodeList as an array of string dofcnShow("...............legWorkCode = " it).}}else  // THIS SHOULD NOT HAPPEN BUT JBEHAVE CODE MIGHT CAUSE IT TO...fcnShow("............Leg: " it.sequenceNumber " ...basePay = " it.basePay " ...payValue = " it.payValue).}}}}}
```

## Dependencias

Esta function llama a:

- [fcn](fcn.md)
- [fcnGetNonRONpremium](fcnGetNonRONpremium.md)
- [fcnGetRapAssociationListAsString](fcnGetRapAssociationListAsString.md)
- [fcnGetSumOfLegBasePayForTrip](fcnGetSumOfLegBasePayForTrip.md)
- [fcnGetSumOfLegPay](fcnGetSumOfLegPay.md)
- [fcnIsReserveBlock](fcnIsReserveBlock.md)
- [fcnIsReserveTrip](fcnIsReserveTrip.md)
- [fcnRoundUpAt2DecimalPlaces](fcnRoundUpAt2DecimalPlaces.md)
- `fcnShow()`
- [main](main.md)

## Llamado por

- [fcnShowSchedulePeriodSummary](fcnShowSchedulePeriodSummary.md)

## Historial de cambios

```
US18085 - Melissa S - 7/29/2014 - Refactored for performance  - Added check for debugMode to avoid looping if not in debugMode
```


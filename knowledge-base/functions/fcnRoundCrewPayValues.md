# fcnRoundCrewPayValues

## Metadata
- **Tipo**: SRL Function
- **Retorna**: void
- **Ubicación**: `CrewRulesRepository\TechnicalLibrary\Pay\CommonPay\Functions\fcnRoundCrewPayValues`

## Propósito
4/5/2016 - CSCH-2723 - Melissa S - Refactored for performance to use while loops and indexes instead of casting to Blaze arrays

## Parámetros

| Nombre | Tipo | Descripción |
|--------|------|-------------|
| aCrewPayResponse | CrewPayResponse | |

## Lógica de negocio

```blaze
aSchedulePeriodPay is some SchedulePeriodPay initially null.aTripPay is some TripPay initially null.aDutyPeriodPay is some DutyPeriodPay initially null.aLegPay is some LegPay initially null.aPayBucket is some PayBucket initially null.perdiemBuckets is a string initially ";FP1BUCKET;FP2BUCKET;FP3BUCKET;FP4BUCKET;FP5BUCKET;FI1BUCKET;FI2BUCKET;FI3BUCKET;FI4BUCKET;FI5BUCKET".spIndex is an integer initially 0.tripIndex is an integer initially 0.dpIndex is an integer initially 0.legIndex is an integer initially 0.bucketIndex is an integer initially 0.if (aCrewPayResponse is not equal to null and    aCrewPayResponse.schedulePeriodPayList is not equal to null and    aCrewPayResponse.schedulePeriodPayList.size() > 0) then{while (spIndex < aCrewPayResponse.schedulePeriodPayList.size()) do {aSchedulePeriodPay = aCrewPayResponse.schedulePeriodPayList.get(spIndex).aSchedulePeriodPay.accruedOji = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.accruedOji).aSchedulePeriodPay.accruedSick = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.accruedSick).aSchedulePeriodPay.adjustmentOji = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.adjustmentOji).aSchedulePeriodPay.adjustmentSick = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.adjustmentSick).aSchedulePeriodPay.aolt = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.aolt).aSchedulePeriodPay.earnedOji = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.earnedOji).aSchedulePeriodPay.earnedSick = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.earnedSick).aSchedulePeriodPay.jaBucket = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.jaBucket).aSchedulePeriodPay.jacBucket = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.jacBucket).aSchedulePeriodPay.jLabelCredits = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.jLabelCredits).aSchedulePeriodPay.monthToDateCredit = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.monthToDateCredit).aSchedulePeriodPay.ojcBucket = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.ojcBucket).aSchedulePeriodPay.ojiBucket = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.ojiBucket).aSchedulePeriodPay.otrBucket = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.otrBucket).aSchedulePeriodPay.pecBucket = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.pecBucket).aSchedulePeriodPay.perBucket = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.perBucket).//////// FOR FLT OPS PERDIEM BUCKET, ROUND TO 3 DECIMAL PLACES OF ACCURACY THEN ROUND TO 2 DECIMAL PLACES OF ACCURACY...////aSchedulePeriodPay.pp1Bucket = math().round(math().round(aSchedulePeriodPay.pp1Bucket, -3), -2).aSchedulePeriodPay.pp2Bucket = math().round(math().round(aSchedulePeriodPay.pp2Bucket, -3), -2).aSchedulePeriodPay.pp3Bucket = math().round(math().round(aSchedulePeriodPay.pp3Bucket, -3), -2).aSchedulePeriodPay.pi1Bucket = math().round(math().round(aSchedulePeriodPay.pi1Bucket, -3), -2).aSchedulePeriodPay.pi2Bucket = math().round(math().round(aSchedulePeriodPay.pi2Bucket, -3), -2).aSchedulePeriodPay.pi3Bucket = math().round(math().round(aSchedulePeriodPay.pi3Bucket, -3), -2).aSchedulePeriodPay.projectedCredit = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.projectedCredit).aSchedulePeriodPay.regBucket = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.regBucket).aSchedulePeriodPay.remainingOji = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.remainingOji).aSchedulePeriodPay.remainingReserveGuaranty = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.remainingReserveGuaranty).aSchedulePeriodPay.remainingSick = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.remainingSick).aSchedulePeriodPay.reserveGuaranty = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.reserveGuaranty).aSchedulePeriodPay.reserveProjectedTotal = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.reserveProjectedTotal).aSchedulePeriodPay.reserveTotal = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.reserveTotal).aSchedulePeriodPay.rgcBucket = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.rgcBucket).aSchedulePeriodPay.sckBucket = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.sckBucket).aSchedulePeriodPay.totalBucket = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.totalBucket).aSchedulePeriodPay.trcBucket = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.trcBucket).aSchedulePeriodPay.trnBucket = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.trnBucket).aSchedulePeriodPay.ubBucket = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.ubBucket).aSchedulePeriodPay.ubcBucket = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.ubcBucket).aSchedulePeriodPay.trnBucket = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.trnBucket).aSchedulePeriodPay.usedOji = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.usedOji).aSchedulePeriodPay.usedSick = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.usedSick).aSchedulePeriodPay.vacBucket = fcnRoundUpAt2DecimalPlaces(aSchedulePeriodPay.vacBucket).if (aSchedulePeriodPay.tripPayList is not equal to null and aSchedulePeriodPay.tripPayList.size() > 0) then{tripIndex = 0.while (tripIndex < aSchedulePeriodPay.tripPayList.size()) do {aTripPay = aSchedulePeriodPay.tripPayList.get(tripIndex).aTripPay.payValue = fcnRoundUpAt2DecimalPlaces(aTripPay.payValue).aTripPay.mdhPayValue = fcnRoundUpAt2DecimalPlaces(aTripPay.mdhPayValue).aTripPay.premiumPayValue = fcnRoundUpAt2DecimalPlaces(aTripPay.premiumPayValue).aTripPay.thisMonthPay = fcnRoundUpAt2DecimalPlaces(aTripPay.thisMonthPay).aTripPay.lastMonthPay = fcnRoundUpAt2DecimalPlaces(aTripPay.lastMonthPay).aTripPay.nextMonthPay = fcnRoundUpAt2DecimalPlaces(aTripPay.nextMonthPay).//////// FOR PERDIEM PROPERTIES, ROUND TO 3 DECIMAL PLACES OF ACCURACY THEN ROUND TO 2 DECIMAL PLACES OF ACCURACY...////aTripPay.oconusPay  = math().round(math().round(aTripPay.oconusPay, -3), -2).aTripPay.conusPay  = math().round(math().round(aTripPay.conusPay, -3), -2).aTripPay.perdiemPay  = math().round(math().round(aTripPay.perdiemPay, -3), -2).//////// FOR INFLIGHT TRIPS....////if (aTripPay.tripPayInflight is not equal to null) then{aTripPay.tripPayInflight.dutyPeriodSum = fcnRoundUpAt2DecimalPlaces(aTripPay.tripPayInflight.dutyPeriodSum).aTripPay.tripPayInflight.tripRIG = fcnRoundUpAt2DecimalPlaces(aTripPay.tripPayInflight.tripRIG).}if (aTripPay.dutyPeriodPayList is not equal to null and aTripPay.dutyPeriodPayList.size() > 0) then{dpIndex = 0.while (dpIndex < aTripPay.dutyPeriodPayList.size()) do {aDutyPeriodPay = aTripPay.dutyPeriodPayList.get(dpIndex).aDutyPeriodPay.payValue = fcnRoundUpAt2DecimalPlaces(aDutyPeriodPay.payValue).aDutyPeriodPay.thisMonthPay = fcnRoundUpAt2DecimalPlaces(aDutyPeriodPay.thisMonthPay).aDutyPeriodPay.lastMonthPay = fcnRoundUpAt2DecimalPlaces(aDutyPeriodPay.lastMonthPay).aDutyPeriodPay.nextMonthPay = fcnRoundUpAt2DecimalPlaces(aDutyPeriodPay.nextMonthPay).aDutyPeriodPay.mdhPayValue = fcnRoundUpAt2DecimalPlaces(aDutyPeriodPay.mdhPayValue).aDutyPeriodPay.premiumPayValue = fcnRoundUpAt2DecimalPlaces(aDutyPeriodPay.premiumPayValue).aDutyPeriodPay.tripExcess = fcnRoundUpAt2DecimalPlaces(aDutyPeriodPay.tripExcess).if (aDutyPeriodPay.legPayList is not equal to null and aDutyPeriodPay.legPayList.size() > 0) then{legIndex = 0.while (legIndex < aDutyPeriodPay.legPayList.size()) do {aLegPay = aDutyPeriodPay.legPayList.get(legIndex).aLegPay.payValue = fcnRoundUpAt2DecimalPlaces(aLegPay.payValue).legIndex += 1.}} dpIndex += 1.}} //APIC-1475 removing zero base value bucketsif(aTripPay.tripPayBuckets is not null) then {for each TripPayBucket in aTripPay.tripPayBuckets.tripPayBucketList as an array of TripPayBucket such that it.baseValue = 0.0 do {aTripPay.tripPayBuckets.tripPayBucketList.remove(it).}}tripIndex += 1.}}//////// FOR INFLIGHT PAYBUCKETS ROUND PAY VALUE TO 2 DECIMAL PLACES OF ACCURACY...DE5442//// TO DO THIS WE ROUND TO 3 DECIMAL PLACES AND THEN ROUND TO 2 DECIMAL PLACES.../////// EXCEPTION IS MADE FOR PERDIEM BUCKETS, THEY ARE TRUNCATED TO 3 DECIMAL PLACES  AND THEN///  ROUNDED TO 2 DECIMAL PLACES... EXAMPLE: FP5BUCKET PAY VALUE 6.0845 SHOULD BE ROUNDED TO 6.08....////if (aSchedulePeriodPay.payBucketList is not equal to null and aSchedulePeriodPay.payBucketList.size() > 0) then{bucketIndex = 0.while (bucketIndex < aSchedulePeriodPay.payBucketList.size()) do {aPayBucket = aSchedulePeriodPay.payBucketList.get(bucketIndex).if (aPayBucket.bucketName = "EXPBUCKET") then {aPayBucket.baseValue = fcnRoundUpAt2DecimalPlaces(aPayBucket.baseValue).aPayBucket.payValue = math().truncate(aPayBucket.payValue, -2).} else if (perdiemBuckets contains match (ignoring case)";"aPayBucket.bucketName";") then {aPayBucket.baseValue = math().truncate(aPayBucket.baseValue, -3).aPayBucket.payValue = math().truncate(aPayBucket.payValue, -3).aPayBucket.baseValue = fcnRoundUpAt2DecimalPlaces(aPayBucket.baseValue).aPayBucket.payValue = fcnRoundUpAt2DecimalPlaces(aPayBucket.payValue).} else {aPayBucket.baseValue = math().round(aPayBucket.baseValue, -3).aPayBucket.payValue = math().round(aPayBucket.payValue, -3).aPayBucket.baseValue = fcnRoundUpAt2DecimalPlaces(aPayBucket.baseValue).aPayBucket.payValue = fcnRoundUpAt2DecimalPlaces(aPayBucket.payValue).}bucketIndex += 1.}}spIndex += 1.}}
```

## Dependencias

Esta function llama a:

- [fcn](fcn.md)
- [fcnRoundUpAt2DecimalPlaces](fcnRoundUpAt2DecimalPlaces.md)
- [main](main.md)

## Historial de cambios

```
4/5/2016 - CSCH-2723 - Melissa S - Refactored for performance to use while loops and indexes instead of casting to Blaze arrays
```


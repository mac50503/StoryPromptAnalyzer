# fcnSetTripSetConusAndOconusDays

## Metadata
- **Tipo**: SRL Function
- **Retorna**: void
- **Ubicación**: `CrewRulesRepository\TechnicalLibrary\Pay\CommonPay\Functions\fcnSetTripSetConusAndOconusDays`

## Propósito
US18085 - Melissa S - 7/29/2014 - Refactored for performance

## Parámetros

| Nombre | Tipo | Descripción |
|--------|------|-------------|
| aTripSetList | List<TripSet> | |
| aGlobalDataCache | GlobalDataCache | |

## Lógica de negocio

```blaze
aTripSet is some TripSet initially a TripSet;prevTripSet is some TripSet initially null;tripSetCounter is an integer initially 0;noOfConusDays is a real initially 0;noOfOconusDays is an real initially 0;tripSetStartTime is an integer initially 0;tripSetEndTime is an integer initially 0;tripSetFirstTrip is some PayTrip initially null;tripSetLastTrip is some PayTrip initially null;prevTripSetEndTime is an integer initially 0;firstDayOverlaps is a boolean initially false;midNightRestStation is some Station initially null.lastDpBeginLocation is some Station initially null.tripSetAdjustedStartDateTime is some DateTime initially null.tripSetAdjustedEndDateTime is some DateTime initially null.prevTripSetAdjustedStartDateTime is some DateTime initially null.prevTripSetAdjustedEndDateTime is some DateTime initially null.beforeEffectiveDateTime is a boolean.while (tripSetCounter < aTripSetList.size())do{aTripSet = aTripSetList.get(tripSetCounter);noOfConusDays = 0;noOfOconusDays = 0;beforeEffectiveDateTime = aTripSet.startDateTime.isBefore(DateTime.newInstance(2015, 12, 1, 0, 0)).if (aTripSet <> null and aTripSet.payTripList<>null and aTripSet.payTripList.size() > 0) then{fcnShow("===>>> Entering fcnSetTripSetConusAndOconusDays with tripSet: " aTripSet.tripSetName).// DE7431 - The change to be made is to change the "half days" from the current 0000-1159 and 1200-2359 to 0001-1200 and 1201-0000.// This new logic will not apply to TripSets whose start DateTime is before December 1 2015 0000.if (beforeEffectiveDateTime) then {tripSetAdjustedStartDateTime = aTripSet.startDateTime.} else if (aTripSet.startDateTime.hourOfDay = 0 and aTripSet.startDateTime.minuteOfHour = 0) then {tripSetAdjustedStartDateTime = aTripSet.startDateTime.minusDays(1).withHourOfDay(23).withMinuteOfHour(59).} else if (aTripSet.startDateTime.hourOfDay = 12 and aTripSet.startDateTime.minuteOfHour = 0) then {tripSetAdjustedStartDateTime = aTripSet.startDateTime.withHourOfDay(11).withMinuteOfHour(59).} else {tripSetAdjustedStartDateTime = aTripSet.startDateTime.}if (beforeEffectiveDateTime) then {tripSetAdjustedEndDateTime = aTripSet.endDateTime.} else if (aTripSet.endDateTime.hourOfDay = 0 and aTripSet.endDateTime.minuteOfHour = 0) then {tripSetAdjustedEndDateTime = aTripSet.endDateTime.minusDays(1).withHourOfDay(23).withMinuteOfHour(59).} else if (aTripSet.endDateTime.hourOfDay = 12 and aTripSet.endDateTime.minuteOfHour = 0) then {tripSetAdjustedEndDateTime = aTripSet.endDateTime.withHourOfDay(11).withMinuteOfHour(59).} else {tripSetAdjustedEndDateTime = aTripSet.endDateTime.}tripSetStartTime = (tripSetAdjustedStartDateTime.hourOfDay*100 + tripSetAdjustedStartDateTime.minuteOfHour);tripSetEndTime = (tripSetAdjustedEndDateTime.hourOfDay*100 + tripSetAdjustedEndDateTime.minuteOfHour);tripSetFirstTrip = aTripSet.payTripList.get(0);tripSetLastTrip = aTripSet.payTripList.get(aTripSet.payTripList.size()-1);prevTripSetEndTime = 0;firstDayOverlaps = false;if (prevTripSet <> null) then {// DE7431 - If a TripSet releases exactly at midnight, this should not be counted as an extra day for conus/oconus purposes.if (beforeEffectiveDateTime) then {prevTripSetAdjustedEndDateTime = prevTripSet.endDateTime.} else if (prevTripSet.endDateTime.hourOfDay = 0 and prevTripSet.endDateTime.minuteOfHour = 0) then {prevTripSetAdjustedEndDateTime = prevTripSet.endDateTime.minusDays(1).withHourOfDay(23).withMinuteOfHour(59).} else if (prevTripSet.endDateTime.hourOfDay = 12 and prevTripSet.endDateTime.minuteOfHour = 0) then {prevTripSetAdjustedEndDateTime = prevTripSet.endDateTime.withHourOfDay(11).withMinuteOfHour(59).} else {prevTripSetAdjustedEndDateTime = prevTripSet.endDateTime.}fcnShow("===>>> prevTripSet endDateTime: " prevTripSet.endDateTime " ...prevTripSetAdjustedEndDateTime: " prevTripSetAdjustedEndDateTime).prevTripSetEndTime = prevTripSetAdjustedEndDateTime.hourOfDay*100 + prevTripSetAdjustedEndDateTime.minuteOfHour;if (aTripSet.startDateTime.toLocalDate().equals(prevTripSetAdjustedEndDateTime.toLocalDate())) then { //It will be 0 when there is no previous tripsetfirstDayOverlaps = true;}}fcnShow("===>>> TripSet datetime start = " aTripSet.startDateTime).fcnShow("===>>> TripSet datetime end = " aTripSet.endDateTime).fcnShow("===>>> Effective datetime start = " tripSetAdjustedStartDateTime).fcnShow("===>>> Effective datetime end = " tripSetAdjustedEndDateTime).//---------------------------TRIP SET HAS ONLY ONE TRIP WITH ONLY ONE DUTY PERIOD------------------------------if (aTripSet.payTripList.size()=1 and aTripSet.payTripList.get(0).dutyPeriodList.size()=1)then{//Exception when previous TripSet ends on same day as that when my TripSet beginsif(firstDayOverlaps=true and prevTripSetEndTime<1200 and tripSetStartTime<1200)then noOfConusDays+=0.5;else if(firstDayOverlaps=true and prevTripSetEndTime>=1200 and tripSetStartTime>=1200)then noOfConusDays+=0.5;else if(tripSetStartTime<1200 and tripSetEndTime<1200)then noOfConusDays+=0.5;else if(tripSetStartTime<1200 and tripSetEndTime>=1200)then noOfConusDays+=1;else if(tripSetStartTime>=1200 and tripSetEndTime>=1200)then noOfConusDays+=0.5;//Accounting for next day if the single Duty Period spans across midnightif (tripSetAdjustedStartDateTime.toLocalDate().isBefore(tripSetAdjustedEndDateTime.toLocalDate()))then noOfConusDays+=1;fcnShow("===>>> TRIP SET HAS ONLY ONE TRIP WITH ONLY ONE DUTY PERIOD noOfConusDays: " noOfConusDays " ...noOfOconusDays: " noOfOconusDays).} //----------------TRIP SET HAS MORE THAN ONE TRIP OR MORE THAN ONE DUTY PERIOD-----------------//---------------------------TRIP SET STARTS AND ENDS IN THE SAME DAY------------------------------else if (tripSetAdjustedStartDateTime.toLocalDate().equals(tripSetAdjustedEndDateTime.toLocalDate())) then{//fcnShow"check -100"); fcnShow"TripSet.startDateTime.toLocalDate()= "aTripSet.startDateTime.toLocalDate().toString());fcnShow"TripSet.endDateTime.toLocalDate()= "tripSetAdjustedEndDateTime.toLocalDate().toString());dpBeginSstation is some Station initially aGlobalDataCache.stationMap.get(tripSetLastTrip.lastDutyPeriod.beginLocation).if(tripSetLastTrip<>null and tripSetLastTrip.lastDutyPeriod <> null)then if(dpBeginSstation =null or dpBeginSstation.isContinentalUS) then{//Station is CONUSif(tripSetStartTime<1200)then if(firstDayOverlaps=true and prevTripSetEndTime<1200)//Exception when previous TripSet ends on same day as that when my TripSet beginsthen noOfConusDays+=0.5;else noOfConusDays+=1;  else if(firstDayOverlaps=true and prevTripSetEndTime>=1200)//Exception when previous TripSet ends on same day as that when my TripSet begins   then noOfConusDays+=0;   else noOfConusDays+=0.5;         }else{// Station is OCONUS  if(tripSetStartTime<1200)  thenif(firstDayOverlaps=true and prevTripSetEndTime<1200)//Exception when previous TripSet ends on same day as that when my TripSet beginsthen noOfOconusDays+=0.5;else noOfOconusDays+=1;  else if(firstDayOverlaps=true and prevTripSetEndTime>=1200)//Exception when previous TripSet ends on same day as that when my TripSet beginsthen noOfOconusDays+=0;   else noOfOconusDays+=0.5;      }fcnShow("===>>> TRIP SET STARTS AND ENDS IN THE SAME DAY noOfConusDays: " noOfConusDays " ...noOfOconusDays: " noOfOconusDays).     }//------------------------------Trip set spans more than a calendar day------------------------------else if (tripSetAdjustedStartDateTime.toLocalDate().isBefore(tripSetAdjustedEndDateTime.toLocalDate())) then{//fcnShow"check -99");//----------------For first calendar day of TripSet-----------------midNightRestStation = aGlobalDataCache.stationMap.get(fcnGetDutyPeriodStartingAfterDateTime(aTripSet,tripSetAdjustedStartDateTime.plusDays(1).withHourOfDay(0).withMinuteOfHour(0)).beginLocation).if(tripSetFirstTrip<>null and tripSetFirstTrip.firstDutyPeriod <> null)thenif(midNightRestStation =null or midNightRestStation.isContinentalUS) then{//fcnShow"check -98");//@@@@@@@@// Mid Night Rest is at a Conus locationif(tripSetStartTime<1200)  then if(firstDayOverlaps=true and prevTripSetEndTime<1200)//Exception when previous TripSet ends on same day as that when my TripSet beginsthen noOfConusDays+=0.5;//fcnShow"check -98");}//@@@@@@@@else noOfConusDays+=1; //fcnShow"check 1");}   //////////////////////////////////////  else if(firstDayOverlaps=true and prevTripSetEndTime>=1200)//Exception when previous TripSet ends on same day as that when my TripSet beginsthen noOfConusDays+=0;else noOfConusDays+=0.5;            } else{// Mid Night Rest is at an Oconus location if(tripSetStartTime<1200)then if(firstDayOverlaps=true and prevTripSetEndTime<1200)//Exception when previous TripSet ends on same day as that when my TripSet begins   then noOfOconusDays+=0.5;   else noOfOconusDays+=1;  else if(firstDayOverlaps=true and prevTripSetEndTime>=1200)//Exception when previous TripSet ends on same day as that when my TripSet begins   then noOfOconusDays+=0;   else noOfOconusDays+=0.5;       }fcnShow("===>>> For first calendar day of TripSet noOfConusDays: " noOfConusDays " ...noOfOconusDays: " noOfOconusDays).//----------------For last calendar day of TripSet----------------lastDpBeginLocation = aGlobalDataCache.stationMap.get(tripSetLastTrip.lastDutyPeriod.beginLocation).if (tripSetLastTrip <> null and tripSetLastTrip.lastDutyPeriod <> null) then { // and (tripSetEndTime>0 and beforeEffectiveDateTime = false)if (lastDpBeginLocation = null or lastDpBeginLocation.isContinentalUS) then {// Last DP begin location is Conus if (tripSetEndTime<1200)  then noOfConusDays+=0.5;  else noOfConusDays+=1; } else {// Last DP begin location is Oconus if (tripSetEndTime<1200) then noOfOconusDays+=0.5;else noOfOconusDays+=1; } fcnShow("===>>> For last calendar day of TripSet noOfConusDays: " noOfConusDays " ...noOfOconusDays: " noOfOconusDays).}//----------------For each middle calendar day of TripSet----------------if (tripSetAdjustedStartDateTime.plusDays(1).toLocalDate().isBefore(tripSetAdjustedEndDateTime.toLocalDate()))then{middleDay is some DateTime initially tripSetAdjustedStartDateTime.plusDays(1);while(middleDay.toLocalDate().isBefore(tripSetAdjustedEndDateTime.toLocalDate()))do{midNightRestStation = aGlobalDataCache.stationMap.get(fcnGetDutyPeriodStartingAfterDateTime(aTripSet,middleDay.plusDays(1).withHourOfDay(0).withMinuteOfHour(0)).beginLocation);if(midNightRestStation=null or midNightRestStation.isContinentalUS) then noOfConusDays+=1;else noOfOconusDays+=1;middleDay=middleDay.plusDays(1);fcnShow("===>>> For each middle calendar day of TripSet noOfConusDays: " noOfConusDays " ...noOfOconusDays: " noOfOconusDays).}}} // End of block for tripSets spanning for more than 1 calendar day}aTripSet.numConusDay = noOfConusDays;aTripSet.numOconusDay = noOfOconusDays;prevTripSet = aTripSet;tripSetCounter+=1;fcnShow("===>>> noOfConusDays: " noOfConusDays " ...noOfOconusDays: " noOfOconusDays).}
```

## Dependencias

Esta function llama a:

- [fcn](fcn.md)
- [fcnGetDutyPeriodStartingAfterDateTime](fcnGetDutyPeriodStartingAfterDateTime.md)
- `fcnShow()`

## Historial de cambios

```
US18085 - Melissa S - 7/29/2014 - Refactored for performance
```


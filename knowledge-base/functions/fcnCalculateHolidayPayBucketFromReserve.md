# fcnCalculateHolidayPayBucketFromReserve

## Metadata
- **Tipo**: SRL Function
- **Retorna**: void
- **Ubicación**: `CrewRulesRepository\TechnicalLibrary\Pay\InflightPay\Functions\fcnCalculateHolidayPayBucketFromReserve`

## Propósito
US16574:Akshay M:10/17/2014

## Parámetros

| Nombre | Tipo | Descripción |
|--------|------|-------------|
| theSchedulePeriodPay | SchedulePeriodPay | |
| aTripList | List<PayTrip> | |
| theSwaHolidayList | List<SwaHoliday> | |
| reportFilterStart | DateTime | |
| reportFilterEnd | DateTime | |

## Lógica de negocio

```blaze
fcnShow("===>>> ENTERING function fcnCalculateHolidayPayBucketFromReserve for SP = " theSchedulePeriodPay.schedulePeriodName " ...HDBUCKET pv = " theSchedulePeriodPay.getPayBucket("HDBUCKET").payValue " ...HDABUCKET pv = " theSchedulePeriodPay.getPayBucket("HDABUCKET").payValue ).// Declare vardayCounter is an integer initially 0.dpCounter is an integer initially 0.legCounter is an integer initially 0.asbCounter is an integer initially 0.bucketSum is a real initially 0.0.hdBucketSum is a real initially 0.0.hdaBucketSum is a real initially 0.0.remainingReserve is a real initially 0.0.hasRON is a boolean initially false.theReserveBlockDay is some ReserveBlockDay.theDutyPeriod is some PayDutyPeriod.theLeg is some PayLeg.theStandbyTrip is some PayTrip.//Loop through each reserve block daywhile(dayCounter < theReserveBlockDayList.count) do {hdBucketSum = 0.0.hdaBucketSum = 0.0.theReserveBlockDay = theReserveBlockDayList[dayCounter].fcnShow("===>>> In reserve block day, reportsOnHoliday = " theReserveBlockDay.reportsOnHoliday);    // If the reserve duty period reports on a holidayif (theReserveBlockDay.reportsOnHoliday) then {for each PayDutyPeriod in theReserveBlockDay.dutyPeriodList as an array of PayDutyPeriod doif it.dutyType = "RON" thenhasRON = true.fcnShow("The reserve block day has RON is " hasRON).//Set to default 6 value if there are no flying tripif ((theReserveBlockDay.airportStandbyTripList.size() = 0 and theReserveBlockDay.dutyPeriodList.size() = 0) or hasRON) then{if(fcnIsDateTimeOnOrAfterConfigCollectionEffectiveDateTime(theReserveBlockDay.calendarDay, "IF_2024_DOR_EFFECTIVE_DATE")) then{fcnShow("===>>> No Flying Trips or Airport Standbys or trip pays RON... Distributing 6.5 to HDBUCKET...").fcnDistributeToPayBucket(theSchedulePeriodPay, "HDBUCKET", 6.5, 1.0, theReserveBlockDay.calendarDay, reportFilterStart, reportFilterEnd, null).}else {fcnShow("===>>> No Flying Trips or Airport Standbys or trip pays RON... Distributing 6.0 to HDBUCKET...").fcnDistributeToPayBucket(theSchedulePeriodPay, "HDBUCKET", 6.0, 1.0, theReserveBlockDay.calendarDay, reportFilterStart, reportFilterEnd, null).}}else{// Check if there is a flying duty periodif (theReserveBlockDay.dutyPeriodList.size() > 0)  then {dpCounter = 0.while (dpCounter < theReserveBlockDay.dutyPeriodList.size()) do{theDutyPeriod = theReserveBlockDay.dutyPeriodList.get(dpCounter).eddLegPremiumSumToDeduct is a real initially 0.0.// Distribute pay from legs...while (legCounter < theDutyPeriod.legList.size()) do{theLeg = theDutyPeriod.legList.get(legCounter).if(fcnIsLegEligibleForEDDPay(theDutyPeriod.payTrip, theLeg, "","fcnCalculateHolidayPayBucketFromReserve")) then {fcnShow("===>>> EDD Leg ==>>"theLeg.sequenceNumber"...theLeg.legPay.payValue==>>"theLeg.legPay.payValue"...theLeg.legPay.basePay==>>"theLeg.legPay.basePay).eddLegPremiumSumToDeduct += math().max(0,(theLeg.legPay.payValue - theLeg.legPay.basePay)).}if (theLeg.legPay.positionA or theDutyPeriod.payTrip.tripPay.tripClass = "P") then // DE6386/DE6468{fcnShow("===>>> HOLIDAY flying leg adding " theLeg.legPay.basePay " to HDABUCKETSUM...").hdaBucketSum += theLeg.legPay.basePay.}else{fcnShow("===>>> HOLIDAY flying leg adding " theLeg.legPay.basePay " to HDBUCKETSUM...").hdBucketSum += theLeg.legPay.basePay.}legCounter+=1.}legCounter=0.// Distribute pay from Duty periods...if (theDutyPeriod.payTrip.tripPay.tripPayInflight.positionA = false) then{fcnShow("===>>> HOLIDAY flying duty period adding " math().max(0, theDutyPeriod.dutyPeriodPay.payValue - hdBucketSum - hdaBucketSum) " to HDBUCKETSUM...").hdBucketSum += math().max(0, theDutyPeriod.dutyPeriodPay.payValue - hdBucketSum - hdaBucketSum). // DE6386}else{fcnShow("===>>> HOLIDAY flying duty period adding " math().max(0, theDutyPeriod.dutyPeriodPay.payValue - hdBucketSum - hdaBucketSum) " to HDABUCKETSUM...").hdaBucketSum += math().max(0, theDutyPeriod.dutyPeriodPay.payValue - hdBucketSum - hdaBucketSum). // DE6386}// Distribute trip RIG...fcnShow("===>>> Number of duty periods is " theDutyPeriod.payTrip.dutyPeriodList.size() " and dp counter = " dpCounter).if(theDutyPeriod = theDutyPeriod.payTrip.lastDutyPeriod) thenif (theDutyPeriod.payTrip.tripPay.tripPayInflight.positionA and theDutyPeriod.payTrip.dutyPeriodList.size() = 1) then{fcnShow("===>>> HOLIDAY flying trip RIG adding " math().max(0, theDutyPeriod.payTrip.tripPay.tripPayInflight.tripRIG) " to HDABUCKETSUM...").hdaBucketSum += math().max(0, theDutyPeriod.payTrip.tripPay.tripPayInflight.tripRIG).}else if (theDutyPeriod.payTrip.tripPay.tripPayInflight.positionA = false and theDutyPeriod.payTrip.dutyPeriodList.size() = 1) then{fcnShow("===>>> HOLIDAY flying trip RIG adding " math().max(0, theDutyPeriod.payTrip.tripPay.tripPayInflight.tripRIG) " to HDBUCKETSUM...").hdBucketSum += math().max(0, theDutyPeriod.payTrip.tripPay.tripPayInflight.tripRIG).}// Exclude premium pay DE6603if (theDutyPeriod.dutyPeriodPay.rigsGreaterThanPremium = false) then {fcnShow("Removing " fcnGetSumOfLegPay(theDutyPeriod.dutyPeriodPay, false, true) - fcnGetSumOfLegPay(theDutyPeriod.dutyPeriodPay, true, true) " from HDABUCKET (" hdaBucketSum ")...").hdaBucketSum = hdaBucketSum - (fcnGetSumOfLegPay(theDutyPeriod.dutyPeriodPay, false, true) - fcnGetSumOfLegPay(theDutyPeriod.dutyPeriodPay, true, true)).fcnShow("hdaBucketSum = " hdaBucketSum).fcnShow("Removing " fcnGetSumOfLegPay(theDutyPeriod.dutyPeriodPay, false, false) - fcnGetSumOfLegPay(theDutyPeriod.dutyPeriodPay, true, false) " from HDBUCKET (" hdBucketSum ")...").hdBucketSum = hdBucketSum - (fcnGetSumOfLegPay(theDutyPeriod.dutyPeriodPay, false, false) - fcnGetSumOfLegPay(theDutyPeriod.dutyPeriodPay, true, false)).fcnShow("hdBucketSum = " hdBucketSum).}// Adjust HD Bucket values when DP RIG Wins with EDD Legsif(eddLegPremiumSumToDeduct > 0.0 and (theDutyPeriod.dutyPeriodPay.rigsGreaterThanPremium)) then {fcnShow("===>>> eddLegPremiumSumToDeduct  ==>>"eddLegPremiumSumToDeduct  "...DP RIG WINS ==>>"theDutyPeriod.dutyPeriodPay.rigsGreaterThanPremium  "...OLD hdBucketSum  ==>>"hdBucketSum   "...OLD hdaBucketSum ==>>"hdaBucketSum).if(hdBucketSum > eddLegPremiumSumToDeduct) then {hdBucketSum = hdBucketSum - eddLegPremiumSumToDeduct.fcnShow("===>>>  NEW hdBucketSum  ==>> "hdBucketSum).}if(hdaBucketSum > eddLegPremiumSumToDeduct) then {hdaBucketSum = hdaBucketSum - eddLegPremiumSumToDeduct.fcnShow("===>>>  NEW hdaBucketSum  ==>> "hdaBucketSum).}}dpCounter+=1.}}// Check if there is any associated Airport standbyif (theReserveBlockDay.airportStandbyTripList.size() > 0) then {   asbCounter = 0.while (asbCounter < theReserveBlockDay.airportStandbyTripList.size()) do {theStandbyTrip = theReserveBlockDay.airportStandbyTripList.get(asbCounter).fcnShow("===>>> HOLIDAY ASB adding " theStandbyTrip.tripPay.thisMonthPay " to ASBBUCKETSUM...").hdBucketSum += theStandbyTrip.tripPay.thisMonthPay.asbCounter+=1.}}if(fcnIsDateTimeOnOrAfterConfigCollectionEffectiveDateTime(theReserveBlockDay.calendarDay, "IF_2024_DOR_EFFECTIVE_DATE")) then{remainingReserve = math().max(0, (6.5 - (hdBucketSum + hdaBucketSum))).}else{remainingReserve = math().max(0, (6 - (hdBucketSum + hdaBucketSum))).}fcnShow("===>>> Found Flying Trips and/or Airport Standbys...").fcnShow("===>>> Distributing hdBucketSum " hdBucketSum " plus remainingReserve " remainingReserve " = "  (hdBucketSum + remainingReserve) " to HDBUCKET...").fcnDistributeToPayBucket(theSchedulePeriodPay, "HDBUCKET", (hdBucketSum + remainingReserve), 1.0, null, null, null, null).fcnShow("===>>> Distributing " hdaBucketSum " to HDABUCKET...").fcnDistributeToPayBucket(theSchedulePeriodPay, "HDABUCKET", hdaBucketSum, 1.0, null, null, null, null).}    }fcnShow("===>>> fcnCalculateHolidayPayBucketFromReserve ...Trip = " theReserveBlockDay.reserveBlock.sequenceNumber "…Reserve Duty period reports on SWA Holiday = " theReserveBlockDay.reportsOnHoliday "..HDBUCKET sum = " theSchedulePeriodPay.getPayBucket("HDBUCKET").payValue "..HDABUCKET sum =" theSchedulePeriodPay.getPayBucket("HDABUCKET").payValue);     dayCounter+=1.}//US18682 : Adding to HD Bucket for airport standby with no associated reserve block dayif (aTripList<>null) then{for each PayTrip in aTripList as an array of PayTrip such that(it.payTripInflight.fallsOnAReserveDay = false and it.nonFlyCode =(ignoring case) ("AS1" or "ASB1" or "ASB2" or "ASB3" or "ASB4")) do{tripReportsOnSwaHoliday is a boolean initially fcnReportsOnHoliday(it.beginDateTime, theSwaHolidayList).if (tripReportsOnSwaHoliday) then { fcnShow("===> Airport Standby Trips with No Associated Reserve Block Day = " it.tripName " …label = " it.assignmentLabel "…Trip reports on SWA Holiday = " tripReportsOnSwaHoliday " ...distributing " it.tripPay.thisMonthPay " to HDBUCKET...").fcnDistributeToPayBucket(theSchedulePeriodPay, "HDBUCKET", it.tripPay.thisMonthPay, 1.0, it.beginDateTime, reportFilterStart, reportFilterEnd, null).} }}fcnShow("===>>> EXITING function fcnCalculateHolidayPayBucketFromReserve for SP = " theSchedulePeriodPay.schedulePeriodName " ...HDBUCKET pv = " theSchedulePeriodPay.getPayBucket("HDBUCKET").payValue " ...HDABUCKET pv = " theSchedulePeriodPay.getPayBucket("HDABUCKET").payValue ).
```

## Dependencias

Esta function llama a:

- [fcn](fcn.md)
- [fcnDistributeToPayBucket](fcnDistributeToPayBucket.md)
- [fcnGetSumOfLegPay](fcnGetSumOfLegPay.md)
- [fcnIsDateTimeOnOrAfterConfigCollectionEffectiveDateTime](fcnIsDateTimeOnOrAfterConfigCollectionEffectiveDateTime.md)
- [fcnIsLegEligibleForEDDPay](fcnIsLegEligibleForEDDPay.md)
- [fcnReportsOnHoliday](fcnReportsOnHoliday.md)
- `fcnShow()`
- [main](main.md)

## Historial de cambios

```
US16574:Akshay M:10/17/2014
US18682:Akshay M:10/21/2014
10/21/2014 Melissa S.  Refactored 4 similar functions into fcnDistributeToPayBucket (Removed fcnAddToPayBucket, fcnCalculateNonFlyPayBucket, fcnCalculatePremiumPayBucket)
DE6172 - Ben Lang - 03/31/2015 - The way how trip RIG is distributed has changed
```


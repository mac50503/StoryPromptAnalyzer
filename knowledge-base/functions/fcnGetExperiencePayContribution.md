# fcnGetExperiencePayContribution

## Metadata
- **Tipo**: SRL Function
- **Retorna**: real
- **Ubicación**: `CrewRulesRepository\TechnicalLibrary\Pay\InflightPay\Functions\fcnGetExperiencePayContribution`

## Propósito
*(Sin descripción)*

## Parámetros

| Nombre | Tipo | Descripción |
|--------|------|-------------|
| aTripPay | TripPay | |
| aSchedulePeriodPay | SchedulePeriodPay | |
| reportFilterStart | DateTime | |
| reportFilterEnd | DateTime | |

## Lógica de negocio

```blaze
fcnShow("===>>> ENTERING fcnGetExperiencePayContribution ...SP = " aSchedulePeriodPay.schedulePeriodName " ...trip = " aTripPay.tripNameAndDate " ...LBL = " aTripPay.assignmentLabel " ...CT = " aTripPay.creditType ).retVal is a real initially 0.0.legsCreditWithCT is a real initially 0.0.legPremium is a real initially 0.0.sumOfDutyAmounts is a real initially 0.0.tripIsContainedWithinSchedulePeriod is a boolean initially fcnDoAllTripDutyPeriodsBeginInSchedulePeriod(aTripPay, aSchedulePeriodPay).firstDutyIsInSp is a boolean initially true.carryOutSpExpValue is a real initially aTripPay.basePay.fcnShow("===>>> trip " aTripPay.tripNameAndDate " ...LBL = " aTripPay.assignmentLabel " ...CT = " aTripPay.creditType " ...starts in SP " aTripPay.startsInSchedulePeriod " ...ends in SP " aTripPay.endsInSchedulePeriod " ...is contained completely within SP " aSchedulePeriodPay.schedulePeriodName "? = " tripIsContainedWithinSchedulePeriod).isReserveBlock is a boolean initially fcnIsReserveBlock(aTripPay.payTrip).dutyBeginsInSp is a boolean initially false.dutyBeginsWithinReportDatRange is a boolean initially false.countOfDutiesWithinReportDateRange is an integer initially 0.includeAnalytics is a boolean initially false.if (aTripPay.tripPayInflightAnalytics <> null) thenincludeAnalytics = true.//fcnShow("===>>> includeAnalytics = " includeAnalytics).remainingTripPay is a real initially 0.0.lastDutyInTrip is some DutyPeriodPay initially null.if (isReserveBlock = false and aTripPay.assignmentLabel <> "I") then{if (aTripPay.dutyPeriodPayList.size() > 0) then{for each DutyPeriodPay in aTripPay.dutyPeriodPayList as an array of DutyPeriodPay do{lastDutyInTrip = it.if (fcnDoesDutyPeriodPayBeginInSchedulePeriodPay(it, aSchedulePeriodPay) and                    fcnIsDateTimeWithinReportFilterRange(it.reportDateTime, reportFilterStart, reportFilterEnd)) then{              countOfDutiesWithinReportDateRange += 1.legsCreditWithCT = 0.0.                             legPremium = 0.0.                                          //Sum the base credit for all the legs with leg work code of CT to later subtract from the amount that will be added to EXPBUCKET. CT should be excluded.                                          for each LegPay in legPayList as an array of LegPay do                                          {                                          if (it.payLeg.legWorkCodeList.contains("CT")) then                                           legsCreditWithCT += it.payValue.                                            if (it.payValue - it.basePay > 0 and it.premiumPayCode <> "" and it.payLeg.legWorkCodeList.contains("CT") = false) then                                                        legPremium += it.payValue - it.basePay.                                          }legsCreditWithCT = fcnRoundUpAt2DecimalPlaces(legsCreditWithCT).legPremium = fcnRoundUpAt2DecimalPlaces(legPremium).}}fcnShow("===>>>  trip = " aTripPay.tripNameAndDate " ...legsCreditWithCT = " legsCreditWithCT " ...legPremium = " legPremium).}//// TRIP IS NOT A CARRYOUTif (tripIsContainedWithinSchedulePeriod = true) then    {fcnShow("===>>> fcnGetExperiencePayContribution - CODE BLOCK 1 - fcnGetExperiencePayContribution ... trip = " aTripPay.tripNameAndDate " ...TRIP DOES NOT CARRY OUT...").allDutyPeriodsWithinReportDateRange is a boolean initially fcnAreAllDutyPeriodsWithinReportDateRange(aTripPay, reportFilterStart, reportFilterEnd).if (allDutyPeriodsWithinReportDateRange = true) then{fcnShow("===>>> fcnGetExperiencePayContribution - CODE BLOCK 2 - fcnGetExperiencePayContribution ... trip = " aTripPay.tripNameAndDate " ...ALL DUTIES ARE WITHIN THE REPORT DATE RANGE...").fcnShow("===>>> fcnGetExperiencePayContribution - CODE BLOCK 2 - highestTripRig = " aTripPay.tripPayInflight.highestTripRig " ... basePay = " aTripPay.basePay).retVal = math().max(aTripPay.tripPayInflight.highestTripRig, aTripPay.basePay).remainingTripPay = retVal.fcnShow("===>>> fcnGetExperiencePayContribution - CODE BLOCK 2 - fcnGetExperiencePayContribution ... trip = " aTripPay.tripNameAndDate " ...credit type = " aTripPay.creditType " ...highestTripRig = " aTripPay.tripPayInflight.highestTripRig " base pay = " aTripPay.basePay " ...RON rig = " aTripPay.tripPayInflight.ronRigTotal " ...retVal now = " retVal).fcnShow("===>>> fcnGetExperiencePayContribution - CODE BLOCK 2 - retVal = " retVal " ....baseDutyPeriodSum = " aTripPay.tripPayInflight.baseDutyPeriodSum).//// DATA ANALYTICS...if (includeAnalytics = true) then{for each DutyPeriodPay in aTripPay.dutyPeriodPayList as an array of DutyPeriodPay do{it.dutyPeriodPayInflightAnalytics.experiencePayTfp = (it.basePay - fcnGetSumOfCharterLegPay(it)) * 1.5.it.dutyPeriodPayInflightAnalytics.experiencePayTfp = fcnRoundUpAt2DecimalPlaces(it.dutyPeriodPayInflightAnalytics.experiencePayTfp).remainingTripPay -= it.basePay.}if (remainingTripPay > 0.0 and lastDutyInTrip <> null) then{lastDutyInTrip.dutyPeriodPayInflightAnalytics.experiencePayTfp += (remainingTripPay * 1.5).lastDutyInTrip.dutyPeriodPayInflightAnalytics.experiencePayTfp = fcnRoundUpAt2DecimalPlaces(lastDutyInTrip.dutyPeriodPayInflightAnalytics.experiencePayTfp).remainingTripPay = 0.0.fcnShow("===>>> added remaining trip pay of " remainingTripPay ", experiencePayTfp for last duty now = " lastDutyInTrip.dutyPeriodPayInflightAnalytics.experiencePayTfp) .}}}else{retVal = math().max(aTripPay.tripPayInflight.highestTripRig, aTripPay.basePay).remainingTripPay = retVal.fcnShow("===>>> fcnGetExperiencePayContribution - CODE BLOCK 3 - fcnGetExperiencePayContribution ... trip = " aTripPay.tripNameAndDate " ...ALL DUTIES ARE NOT WITHIN THE REPORT DATE RANGE... starting retVal is highest trip rig of " retVal).for each DutyPeriodPay in aTripPay.dutyPeriodPayList as an array of DutyPeriodPay do{//// DATA ANALYTICS...if (includeAnalytics = true and                                                  fcnDoesDutyPeriodPayBeginInSchedulePeriodPay(it, aSchedulePeriodPay) = true and    fcnIsDateTimeWithinReportFilterRange(it.reportDateTime, reportFilterStart, reportFilterEnd) = true) then{it.dutyPeriodPayInflightAnalytics.experiencePayTfp = (it.basePay - fcnGetSumOfCharterLegPay(it)) * 1.5.it.dutyPeriodPayInflightAnalytics.experiencePayTfp = fcnRoundUpAt2DecimalPlaces(it.dutyPeriodPayInflightAnalytics.experiencePayTfp).remainingTripPay -= it.basePay.}if (fcnDoesDutyPeriodPayBeginInSchedulePeriodPay(it, aSchedulePeriodPay) = false or     fcnIsDateTimeWithinReportFilterRange(it.reportDateTime, reportFilterStart, reportFilterEnd) = false) then{retVal -= it.basePay.remainingTripPay -= it.basePay.fcnShow("===>>> fcnGetExperiencePayContribution - CODE BLOCK 4 - reducing retVal  by DP " it.sequenceNumber "'s basePay of " it.basePay " ...value now = " retVal).}}if (includeAnalytics = true and remainingTripPay > 0.0 and remainingTripPay > 0.0 and lastDutyInTrip <> null and                                      fcnDoesDutyPeriodPayBeginInSchedulePeriodPay(lastDutyInTrip, aSchedulePeriodPay) = true and    fcnIsDateTimeWithinReportFilterRange(lastDutyInTrip.reportDateTime, reportFilterStart, reportFilterEnd) = true) then{lastDutyInTrip.dutyPeriodPayInflightAnalytics.experiencePayTfp += (remainingTripPay * 1.5).lastDutyInTrip.dutyPeriodPayInflightAnalytics.experiencePayTfp = fcnRoundUpAt2DecimalPlaces(lastDutyInTrip.dutyPeriodPayInflightAnalytics.experiencePayTfp).remainingTripPay = 0.0.fcnShow("===>>> added remaining trip pay of " remainingTripPay ", experiencePayTfp for last duty now = " lastDutyInTrip.dutyPeriodPayInflightAnalytics.experiencePayTfp) .}}}//// TRIP IS A CARRYOUTelse if (aTripPay.dutyPeriodPayList.size() > 0) then    {fcnShow("===>>> fcnGetExperiencePayContribution - CODE BLOCK 5 - fcnGetExperiencePayContribution ... trip = " aTripPay.tripNameAndDate " ...TRIP DOES CARRY OUT...").////    TRIP IS FORCED OR PRORATEDif (aTripPay.creditType =(ignoring case) ("F" or "P")) then{fcnShow("===>>> fcnGetExperiencePayContribution - CODE BLOCK 6 - processing carry-out forced trip " aTripPay.tripNameAndDate " ...credit type = " aTripPay.creditType).for each DutyPeriodPay in aTripPay.dutyPeriodPayList as an array of DutyPeriodPay do{dutyBeginsInSp = fcnDoesDutyPeriodPayBeginInSchedulePeriodPay(it, aSchedulePeriodPay).dutyBeginsWithinReportDatRange = fcnIsDateTimeWithinReportFilterRange(it.reportDateTime, reportFilterStart, reportFilterEnd).if (aTripPay.payTrip.firstDutyPeriod = it.payDutyPeriod and dutyBeginsInSp  = false) thenfirstDutyIsInSp = false.fcnShow("===>>> for trip " aTripPay.tripNameAndDate " first duty is in SP " aSchedulePeriodPay.schedulePeriodName " = " firstDutyIsInSp).if (dutyBeginsInSp = true and dutyBeginsWithinReportDatRange = true) then{sumOfDutyAmounts += it.basePay.fcnShow("===>>> fcnGetExperiencePayContribution - CODE BLOCK 7 - adding DP " it.sequenceNumber "'s basePay of " it.basePay " to sumOfDutyAmounts ...value now = " sumOfDutyAmounts).//// DATA ANALYTICS...if (includeAnalytics = true) then {it.dutyPeriodPayInflightAnalytics.experiencePayTfp = (it.basePay - fcnGetSumOfCharterLegPay(it)) * 1.5.it.dutyPeriodPayInflightAnalytics.experiencePayTfp = fcnRoundUpAt2DecimalPlaces(it.dutyPeriodPayInflightAnalytics.experiencePayTfp).}}elsefcnShow("===>>> fcnGetExperiencePayContribution - CODE BLOCK 8 - skipping DP " it.sequenceNumber " ...dutyBeginsInSp = " dutyBeginsInSp " ...dutyBeginsWithinReportD = " dutyBeginsWithinReportDatRange " ... DP report = " it.reportDateTime " ...SP start = " aSchedulePeriodPay.schedulePeriodStart " ...SP end = " aSchedulePeriodPay.schedulePeriodEnd).if (firstDutyIsInSp = false and dutyBeginsInSp = false) then{carryOutSpExpValue -= it.basePay.fcnShow("===>>> fcnGetExperiencePayContribution - CODE BLOCK 9 - reducing carryOutSpExpValue  by DP " it.sequenceNumber "'s basePay of " it.basePay " ...value now = " carryOutSpExpValue).}remainingTripPay -= it.basePay.}if (includeAnalytics = true and remainingTripPay > 0.0 and remainingTripPay > 0.0 and lastDutyInTrip <> null and                                           fcnDoesDutyPeriodPayBeginInSchedulePeriodPay(lastDutyInTrip, aSchedulePeriodPay) = true and    fcnIsDateTimeWithinReportFilterRange(lastDutyInTrip.reportDateTime, reportFilterStart, reportFilterEnd) = true) then{lastDutyInTrip.dutyPeriodPayInflightAnalytics.experiencePayTfp += (remainingTripPay * 1.5).lastDutyInTrip.dutyPeriodPayInflightAnalytics.experiencePayTfp = fcnRoundUpAt2DecimalPlaces(lastDutyInTrip.dutyPeriodPayInflightAnalytics.experiencePayTfp).fcnShow("===>>> added remaining trip pay of " remainingTripPay ", experiencePayTfp for last duty now = " lastDutyInTrip.dutyPeriodPayInflightAnalytics.experiencePayTfp) .remainingTripPay = 0.0.}if (firstDutyIsInSp = true) then{retVal = math().min(sumOfDutyAmounts, aTripPay.basePay).fcnShow("===>>> fcnGetExperiencePayContribution - CODE BLOCK 10 - first duty IS In Sp " aSchedulePeriodPay.schedulePeriodName " so setting retVal to min(sumOfDutyAmounts, aTripPay.basePay) ... retVal = " retVal).}else{retVal = carryOutSpExpValue.fcnShow("===>>> fcnGetExperiencePayContribution - CODE BLOCK 11 - first duty IS NOT In Sp " aSchedulePeriodPay.schedulePeriodName " so setting retVal to carryOutSpExpValue ... retVal = " retVal).}}else////    TRIP IS NOT FORCED OR PRORATED{carryOutSpExpValue = math().max(aTripPay.tripPayInflight.highestTripRig, aTripPay.basePay).fcnShow("===>>> fcnGetExperiencePayContribution - CODE BLOCK 12 - processing carry-out NON-forced trip " aTripPay.tripNameAndDate " ...credit type = " aTripPay.creditType).for each DutyPeriodPay in aTripPay.dutyPeriodPayList as an array of DutyPeriodPay do{dutyBeginsInSp = fcnDoesDutyPeriodPayBeginInSchedulePeriodPay(it, aSchedulePeriodPay).dutyBeginsWithinReportDatRange = fcnIsDateTimeWithinReportFilterRange(it.reportDateTime, reportFilterStart, reportFilterEnd).if (aTripPay.payTrip.firstDutyPeriod = it.payDutyPeriod and dutyBeginsInSp = false) thenfirstDutyIsInSp = false.fcnShow("===>>> for trip " aTripPay.tripNameAndDate " first duty is in SP " aSchedulePeriodPay.schedulePeriodName " = " firstDutyIsInSp).if (dutyBeginsInSp = true and dutyBeginsWithinReportDatRange = true) then{sumOfDutyAmounts += it.basePay.fcnShow("===>>> fcnGetExperiencePayContribution - CODE BLOCK 13 - adding DP " it.sequenceNumber "'s base pay of " it.basePay " to sumOfDutyAmounts ...value now = " sumOfDutyAmounts).//// DATA ANALYTICS...if (includeAnalytics = true) then {it.dutyPeriodPayInflightAnalytics.experiencePayTfp = (it.basePay - fcnGetSumOfCharterLegPay(it)) * 1.5.it.dutyPeriodPayInflightAnalytics.experiencePayTfp = fcnRoundUpAt2DecimalPlaces(it.dutyPeriodPayInflightAnalytics.experiencePayTfp).}}elsefcnShow("===>>> fcnGetExperiencePayContribution - CODE BLOCK 14 - skipping DP " it.sequenceNumber " ...dutyBeginsInSp = " dutyBeginsInSp " ...dutyBeginsWithinReportD = " dutyBeginsWithinReportDatRange " ... DP report = " it.reportDateTime " ...SP start = " aSchedulePeriodPay.schedulePeriodStart " ...SP end = " aSchedulePeriodPay.schedulePeriodEnd).if (firstDutyIsInSp = false and dutyBeginsInSp = false) then{carryOutSpExpValue -= it.basePay.fcnShow("===>>> fcnGetExperiencePayContribution - CODE BLOCK 15 - reducing carryOutSpExpValue  by DP " it.sequenceNumber "'s base pay of " it.basePay " ...value now = " carryOutSpExpValue).}remainingTripPay = 0.0.}if (firstDutyIsInSp = true) then{retVal = math().min(sumOfDutyAmounts, aTripPay.basePay).retVal = math().min(retVal, aTripPay.tripPayInflight.highestTripRig).fcnShow("===>>> fcnGetExperiencePayContribution - CODE BLOCK 16 - setting retVal to min of sumOfDutyAmounts, aTripPay.basePay and highestTripRig ...retVal = " retVal).}else{retVal = carryOutSpExpValue.fcnShow("===>>> fcnGetExperiencePayContribution - CODE BLOCK 17 - setting retVal to carryOutSpExpValue ...retVal = " retVal).}if (includeAnalytics = true and remainingTripPay > 0.0 and remainingTripPay > 0.0 and lastDutyInTrip <> null and                                           fcnDoesDutyPeriodPayBeginInSchedulePeriodPay(lastDutyInTrip, aSchedulePeriodPay) = true and    fcnIsDateTimeWithinReportFilterRange(lastDutyInTrip.reportDateTime, reportFilterStart, reportFilterEnd) = true) then{lastDutyInTrip.dutyPeriodPayInflightAnalytics.experiencePayTfp += (remainingTripPay * 1.5).lastDutyInTrip.dutyPeriodPayInflightAnalytics.experiencePayTfp = fcnRoundUpAt2DecimalPlaces(lastDutyInTrip.dutyPeriodPayInflightAnalytics.experiencePayTfp).fcnShow("===>>> added remaining trip pay of " remainingTripPay ", experiencePayTfp for last duty now = " lastDutyInTrip.dutyPeriodPayInflightAnalytics.experiencePayTfp) .remainingTripPay = 0.0.}}} if (legsCreditWithCT > 0.0) then{retVal -= legsCreditWithCT.fcnShow("===>>> fcnGetExperiencePayContribution - CODE BLOCK 18 - fcnGetExperiencePayContribution ... trip = " aTripPay.tripNameAndDate " ...removing charter leg pay of " legsCreditWithCT " ...retVal now = " retVal).}}if (countOfDutiesWithinReportDateRange = 0) then{retVal = 0.0.fcnShow("===>>> fcnGetExperiencePayContribution - CODE BLOCK 19 - fcnGetExperiencePayContribution ... trip = " aTripPay.tripNameAndDate " ...setting retVAl to 0.0 because no duties are within the report date range").}if (retVal > 0.0) thenretVal = fcnRoundUpAt2DecimalPlaces(retVal).fcnShow("===>>> EXITING fcnGetExperiencePayContribution ...SP = " aSchedulePeriodPay.schedulePeriodName " ...trip = " aTripPay.tripNameAndDate " ...LBL = " aTripPay.assignmentLabel " ...returning " retVal).return retVal.
```

## Dependencias

Esta function llama a:

- [fcn](fcn.md)
- [fcnAreAllDutyPeriodsWithinReportDateRange](fcnAreAllDutyPeriodsWithinReportDateRange.md)
- [fcnDoAllTripDutyPeriodsBeginInSchedulePeriod](fcnDoAllTripDutyPeriodsBeginInSchedulePeriod.md)
- [fcnDoesDutyPeriodPayBeginInSchedulePeriodPay](fcnDoesDutyPeriodPayBeginInSchedulePeriodPay.md)
- [fcnGetSumOfCharterLegPay](fcnGetSumOfCharterLegPay.md)
- [fcnIsDateTimeWithinReportFilterRange](fcnIsDateTimeWithinReportFilterRange.md)
- [fcnIsReserveBlock](fcnIsReserveBlock.md)
- [fcnRoundUpAt2DecimalPlaces](fcnRoundUpAt2DecimalPlaces.md)
- `fcnShow()`
- [main](main.md)
- [ping](ping.md)

## Llamado por

- [fcnCalculateExperiencePayBucket](fcnCalculateExperiencePayBucket.md)


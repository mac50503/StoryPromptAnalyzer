# fcnCalculateTripContributionForProductivityPay

## Metadata
- **Tipo**: SRL Function
- **Retorna**: real
- **Ubicación**: `CrewRulesRepository\TechnicalLibrary\Pay\InflightPay\Functions\fcnCalculateTripContributionForProductivityPay`

## Propósito
*(Sin descripción)*

## Parámetros

| Nombre | Tipo | Descripción |
|--------|------|-------------|
| aTripPay | TripPay | |
| aSchedulePeriodPay | SchedulePeriodPay | |
| reportDateRangeFilterStart | DateTime | |
| reportDateRangeFilterEnd | DateTime | |
| useReserveMethodology | boolean | |

## Lógica de negocio

```blaze
//////// this function calculates the trip level contribution for productivity pay for trips with credit type of F or P or Q...//////fcnShow("===>>> ENTERING fcnCalculateTripContributionForProductivityPay for trip " aTripPay.tripNameAndDate " ...LBL = " aTripPay.assignmentLabel " ...passed CT = " aTripPay.payTrip.creditType " ...CT = " aTripPay.creditType).contribution is a real initially 0.0.dutyPeriodContribution is a real initially 0.0.skipThisTrip is a boolean initially false.if (aTripPay <> null and aTripPay.payTrip <> null and      useReserveMethodology and     fcnIsReserveTrip(aTripPay.payTrip) and     fcnDoAllDutyPeriodsInSpHaveRapAssociations(aTripPay.payTrip, aSchedulePeriodPay)) then{skipThisTrip = true.//fcnShow("===>>> skipping trip " tripName " because it is a reserve trip and all of its duty periods have RAP associations...").}if (aTripPay <> null and aTripPay.payTrip <> null and skipThisTrip = false) then{ allDutiesHaveRapAssociations is a boolean initially fcnDoAllDutyPeriodsHaveRapAssociations(aTripPay.payTrip).tripStr is a string initially "trip".carryIn is a boolean initially false.carryOut is a boolean initially false.tripBeginsWithinReportDateRange is a boolean initially fcnIsDateTimeWithinReportFilterRange(aTripPay.beginDateTime, reportDateRangeFilterStart, reportDateRangeFilterEnd).if (aTripPay.startsInSchedulePeriod = aTripPay.endsInSchedulePeriod) then{carryOut = false.carryIn = false.}else if (aTripPay.startsInSchedulePeriod = aSchedulePeriodPay.schedulePeriodName) then{tripStr = "carry-out trip".carryOut = true.carryIn = false.}else{tripStr = "carrry-in trip".carryIn = true.carryOut = false. }legPaySumForTrip is a real initially fcnGetSumOfLegPayForTrip(aTripPay).legBasePaySumForTrip is a real initially fcnGetSumOfLegBasePayForTrip(aTripPay.payTrip).if (carryIn = false and carryOut = false) then   //// TRIP IS CONTAINED COMPLETELY WITHIN THE SCHEDULE PERIOD...{if (aTripPay.payTrip.creditType = (ignoring case) "F") then //// HANDLE FORCED TRIPS COMPLETELY CONTAINED INSIDE THE SCHEDULE PERIOD...{contribution = aTripPay.payTrip.basePay.fcnShow("===>>> setting trip " aTripPay.tripNameAndDate " ...LBL = " aTripPay.assignmentLabel " ...CT = " aTripPay.creditType " ...contribution to base pay of  " contribution).if (aTripPay.dutyPeriodPayList.size() <> 0) then{for each DutyPeriodPay in aTripPay.dutyPeriodPayList as an array of DutyPeriodPay do{if (fcnIsDateTimeWithinReportFilterRange(fcnGetFirstLegOfDutyPeriod(it.payDutyPeriod).scheduledDepartureDateTime, reportDateRangeFilterStart, reportDateRangeFilterEnd) = false or     fcnDoesDutyPeriodPayBeginInSchedulePeriodPay(it, aSchedulePeriodPay) = false) then{contribution -= it.basePay.//fcnShow("===>>> subtracting DP " it.sequenceNumber " base pay of " it.basePay " from trip " tripName " contribution because DP does not start in SP or date range filter ...value now = " contribution).}if (aTripPay.assignmentLabel <> ("J" and "U" and "V" and "D" and "E") and    fcnIsDateTimeWithinReportFilterRange(fcnGetFirstLegOfDutyPeriod(it.payDutyPeriod).scheduledDepartureDateTime, reportDateRangeFilterStart, reportDateRangeFilterEnd) and    fcnDoesDutyPeriodPayBeginInSchedulePeriodPay(it, aSchedulePeriodPay)) then{if (it.payDutyPeriod.dutyType <> (ignoring case) "RON" and     it.payDutyPeriod.creditType <> (ignoring case) ("F" and "P") and     it.creditType <> (ignoring case) ("F" and "P") and     it.thisMonthPay > math().max(it.dutyHourRatio, it.dutyPeriodMinimum) and                                                                                             math().max(it.dutyHourRatio, it.dutyPeriodMinimum) > it.sumLegBaseCredits and     it.rigsGreaterThanPremium = false) then{contribution -= math().max(it.dutyHourRatio, it.dutyPeriodMinimum) - it.sumLegBaseCredits.//// SUBTRACT DUTY RIG WHEN DP PAYS MORE THAN (MAX OF DHR  and DPM) and  (MAX OF DHR  and DPM) > SUM OF LEG BASE CREDITSdutyRigOverage is a real initially math().max(it.dutyHourRatio, it.dutyPeriodMinimum) - it.sumLegBaseCredits.dutyRigOverage = fcnRoundUpAt2DecimalPlaces(dutyRigOverage).fcnShow("===>>> Trip " aTripPay.tripNameAndDate " LBL = " aTripPay.assignmentLabel " ...CT = " it.creditType " ...base pay = " aTripPay.basePay " ..this month pay = " aTripPay.thisMonthPay).fcnShow("===>>> DP " it.sequenceNumber " ...CT = " it.creditType " base pay = " it.basePay " ...this month pay = " it.thisMonthPay " ...DHR = " it.dutyHourRatio " sum legs base = " it.sumLegBaseCredits).fcnShow("===>>> subtracting DP " it.sequenceNumber " duty rig overage of " dutyRigOverage " from trip " aTripPay.tripNameAndDate " contribution ...value now = " contribution).}}}}}else //// HANDLE NON-FORCED TRIPS COMPLETELY CONTAINED INSIDE THE SCHEDULE PERIOD...{if (aTripPay.creditType  = "G" and    aTripPay.basePay > legPaySumForTrip and    aTripPay.basePay > aTripPay.tripPayInflight.dutyPeriodSum) then{//fcnShow("===>>> calculated trip credit type = G and trip base pay of " aTripPay.basePay  " > legPaySumForTrip of " legPaySumForTrip " and >  dutyPeriodSum of " aTripPay.tripPayInflight.dutyPeriodSum " so contributing trip base pay of " aTripPay.payTrip.basePay).contribution = aTripPay.payTrip.basePay.}else if (aTripPay.creditType  = "G" and             legPaySumForTrip >= aTripPay.basePay and             legPaySumForTrip >= aTripPay.tripPayInflight.dutyPeriodSum) then{//fcnShow("===>>> calculated trip credit type = G and legPaySumForTrip of " legPaySumForTrip " >= trip base pay of " aTripPay.basePay  " and >=  dutyPeriodSum of " aTripPay.tripPayInflight.dutyPeriodSum " so contributing legBasePaySumForTrip pay of " legBasePaySumForTrip).contribution = legBasePaySumForTrip.}else if (aTripPay.creditType  = "G" and             legPaySumForTrip >= aTripPay.basePay and             legPaySumForTrip < aTripPay.tripPayInflight.dutyPeriodSum) then{//fcnShow("===>>> calculated trip credit type = G and legPaySumForTrip of " legPaySumForTrip " >= trip base pay of " aTripPay.basePay  " and dutyPeriodSum >  legPaySumForTrip of " legPaySumForTrip " so contributing baseDutyPeriodSums of " aTripPay.tripPayInflight.baseDutyPeriodSum).contribution = aTripPay.tripPayInflight.baseDutyPeriodSum.}else if (aTripPay.creditType  = "P") then{if (aTripPay.tripPayInflight.rigsGreaterThanPremium = true) then{contribution = fcnMaxOf3Numbers(aTripPay.basePay, aTripPay.tripPayInflight.baseDutyPeriodSum, legBasePaySumForTrip).if (aTripPay.tripPayInflight.isReserveTrip = true) thencontribution += fcnGetSumOfDutyRig(aTripPay).    //// DE7552}else{dutyPeriodContribution = 0.0.for each DutyPeriodPay in aTripPay.dutyPeriodPayList as an array of DutyPeriodPay do{if (it.rigsGreaterThanPremium = false) then{dutyPeriodContribution = it.sumLegBaseCredits + it.dutyPeriodRig.}elsedutyPeriodContribution = it.basePay.contribution += dutyPeriodContribution.//fcnShow("===>>> SP = " aSchedulePeriodPay.schedulePeriodName " ... " tripStr " " tripNaTripPay.tripNameAndDateame " with label = " aTripPay.assignmentLabel " ...duty period " it.sequenceNumber //" contributes " dutyPeriodContribution " ... total trip contribution now = " contribution).}}}else if (aTripPay.payValue > aTripPay.basePay and             aTripPay.payValue > aTripPay.tripPayInflight.thrValue) then{//fcnShow("===>>> Trip Pay of " aTripPay.payValue  " > than trip base pay of "  aTripPay.basePay " and THR of " aTripPay.tripPayInflight.thrValue  " so contributing sum of leg base pay of " legBasePaySumForTrip).if(aTripPay.basePay > legBasePaySumForTrip) then {contribution = aTripPay.basePay.}else {contribution = legBasePaySumForTrip.}}else if (aTripPay.tripPayInflight.tripRIG > 0.0) then{//fcnShow("===>>> Trip Rig for Trip " aTripPay.tripNameAndDate " = " aTripPay.tripPayInflight.tripRIG " so contributing trip base pay of " aTripPay.payTrip.basePay).contribution = aTripPay.payTrip.basePay.}else if (aTripPay.tripPayInflight.dutyPeriodSum > legPaySumForTrip) then{//fcnShow("===>>> dutyPeriodSumFor Trip " aTripPay.tripNameAndDate " = " aTripPay.tripPayInflight.baseDutyPeriodSum " ...legPaySumForTrip = " legPaySumForTrip " so contributing trip base pay of " aTripPay.payTrip.basePay).contribution = aTripPay.payTrip.basePay.}else if (legPaySumForTrip > aTripPay.tripPayInflight.baseDutyPeriodSum) then{//fcnShow("===>>> legPaySumForTrip Trip " aTripPay.tripNameAndDate " = " legPaySumForTrip " ...baseDutyPeriodSum = " aTripPay.tripPayInflight.baseDutyPeriodSum " so contributing sum of leg base pay of " legBasePaySumForTrip).contribution = legBasePaySumForTrip.}else if (aTripPay.tripPayInflight.thisMonthLegPremium > 0) then{dutyPeriodContribution = 0.0.for each DutyPeriodPay in aTripPay.dutyPeriodPayList as an array of DutyPeriodPay do{//fcnShow("===>>> SP = " aSchedulePeriodPay.schedulePeriodName " ... " tripStr " " aTripPay.tripNameAndDate " ...LBL = " aTripPay.assignmentLabel " ...CT = " aTripPay.creditType " ...duty period " it.sequenceNumber " ...DHR = " it.dutyHourRatio " ...sumLegBaseCredits * 1.5 = "sumLegBaseCredits * 1.5).if (it.thisMonthPay > it.sumLegTotalCredits) then{dutyPeriodContribution = it.sumLegBaseCredits + it.dutyPeriodRig.//fcnShow("===>>> SP = " aSchedulePeriodPay.schedulePeriodName " ... " tripStr " " aTripPay.tripNameAndDate " with label = " aTripPay.assignmentLabel " ...duty period " //it.sequenceNumber " ...thisMonthPay = " it.thisMonthPay " ...sumLegBaseTotalCredits  = " it.sumLegTotalCredits).//fcnShow("===>>> adding straight pay of legs (" it.sumLegBaseCredits ") plus duty rig ( " it.dutyPeriodRig ") to contribution ...value now = " dutyPeriodContribution).}else if ((it.sumLegBaseCredits * 1.5) > it.dutyHourRatio) thendutyPeriodContribution = it.sumLegBaseCredits.elsedutyPeriodContribution = it.basePay.contribution += dutyPeriodContribution.//fcnShow("===>>> SP = " aSchedulePeriodPay.schedulePeriodName " ... " tripStr " " aTripPay.tripNameAndDate " with label = " aTripPay.assignmentLabel " ...duty period " it.sequenceNumber //" contributes " dutyPeriodContribution " ... total trip contribution now = " contribution).}}elsecontribution = aTripPay.payTrip.basePay.}if (aTripPay.tripPayInflight.ronRigTotal <> 0 and aTripPay.assignmentLabel = ("J" or "U" or "V" or "D" or "E")) then{contribution += aTripPay.tripPayInflight.ronRigTotal.fcnShow("===>>> Completely contained in the SP...... Adding RON-RIG of Duty rig of " aTripPay.tripPayInflight.ronRigTotal " to trip " aTripPay.tripNameAndDate "'s contribution ... value now = " contribution " ...fcnCalculateTripContributionForProductivityPay").}}else if (carryIn = false and carryOut = true) then   //// TRIP IS CARRYING OUT OF THE SCHEDULE PERIOD...{//// HANDLE TRIPS CARRYING OUT OF THE SCHEDULE PERIOD...if (aTripPay.creditType = "F" or aTripPay.tripPayInflight.thisMonthLegPremium > 0) then{for each DutyPeriodPay in aTripPay.dutyPeriodPayList as an array of DutyPeriodPay do{dutyPeriodContribution = 0.0.if (fcnIsDateTimeWithinReportFilterRange(fcnGetFirstLegOfDutyPeriod(it.payDutyPeriod).scheduledDepartureDateTime, reportDateRangeFilterStart, reportDateRangeFilterEnd)) then{reportsInSP is a boolean initially false.reportsInSP = fcnDoesDutyPeriodPayBeginInSchedulePeriodPay(it, aSchedulePeriodPay).if (reportsInSP) then{if (it.rigsGreaterThanPremium = false) thendutyPeriodContribution = it.sumLegBaseCredits.else if (it.creditType = (ignoring case) "P") thendutyPeriodContribution += math().max(it.basePay, it.sumLegBaseCredits).else if (it.creditType = (ignoring case) "M") thendutyPeriodContribution += math().max(4.0, it.sumLegBaseCredits).elsedutyPeriodContribution = it.sumLegBaseCredits + it.dutyPeriodRig. //// DE7328 added duty rigcontribution += dutyPeriodContribution.//fcnShow("===>>> SP = " aSchedulePeriodPay.schedulePeriodName " ... " tripStr " " aTripPay.tripNameAndDate " with label = " aTripPay.assignmentLabel " ...duty period " it.sequenceNumber " ...reports in SP = " reportsInSP //" ...sumLegBaseCredits = " sumLegBaseCredits " ...contribution = " contribution).}if (aTripPay.creditType = "F" and      aTripPay.assignmentLabel = ("J" or "U" or "V" or "D" or "E") and         it.thisMonthPay > it.sumLegTotalCredits) then{dutyPeriodContribution = it.thisMonthPay - it.sumLegTotalCredits.contribution += dutyPeriodContribution.}//fcnShow("===>>> SP = " aSchedulePeriodPay.schedulePeriodName " ... " tripStr " " aTripPay.tripNameAndDate  " ...LBL = " aTripPay.assignmentLabel " ...CT = " aTripPay.creditType " ...duty period " it.sequenceNumber //" contributes " dutyPeriodContribution " ... total trip contribution now = " contribution).}}}else{tripBasePlusPremium is a real initially aTripPay.basePay * 1.5.ineligibleDutySum is a real initially 0.0.for each DutyPeriodPay in aTripPay.dutyPeriodPayList as an array of DutyPeriodPay do{reportsInSP is a boolean initially false.reportsInSP = fcnDoesDutyPeriodPayBeginInSchedulePeriodPay(it, aSchedulePeriodPay).if (reportsInSP = false) then{fcnShow("===>>> CB Y SP = " aSchedulePeriodPay.schedulePeriodName " ... " tripStr " " aTripPay.tripNameAndDate " LBL = " aTripPay.assignmentLabel " ...duty period " it.sequenceNumber " ...reports in SP = " reportsInSP " ...sumLegBaseCredits = " sumLegBaseCredits).ineligibleDutySum += math().max(it.sumLegBaseCredits,  it.payDutyPeriod.basePay) * 1.5}}contribution = (tripBasePlusPremium - ineligibleDutySum) / 1.5.contribution = fcnRoundUpAt2DecimalPlaces(contribution).//fcnShow("===>>> SP = " aSchedulePeriodPay.schedulePeriodName " ... " tripStr " " aTripPay.tripNameAndDate  " ...LBL = " aTripPay.assignmentLabel " ...CT = " aTripPay.creditType " contributes (trip base plus premium - ineligible duty amount) / 1.5 = (" tripBasePlusPremium " - " ineligibleDutySum ") / 1.5 = " contribution)..}}else if (carryIn = true and carryOut = false) then   //// TRIP IS CARRYING INTO THE SCHEDULE PERIOD...{//// HANDLE TRIPS CARRYING INTO THE SCHEDULE PERIOD...fcnShow("fcnCalculateTripContributionForProductivityPay ... handling carry-in trip " aTripPay.tripNameAndDate).if (aTripPay.creditType = "P" or aTripPay.basePay <= aTripPay.tripPayInflight.baseDutyPeriodSum) then{for each DutyPeriodPay in aTripPay.dutyPeriodPayList as an array of DutyPeriodPay do{dutyPeriodContribution = 0.0.if (fcnIsDateTimeWithinReportFilterRange(fcnGetFirstLegOfDutyPeriod(it.payDutyPeriod).scheduledDepartureDateTime, reportDateRangeFilterStart, reportDateRangeFilterEnd)) then{reportsInSP is a boolean initially false.reportsInSP = fcnDoesDutyPeriodPayBeginInSchedulePeriodPay(it, aSchedulePeriodPay).//fcnShow("===>>> SP = " aSchedulePeriodPay.schedulePeriodName " ... " tripStr " " aTripPay.tripNameAndDate  " ...LBL = " aTripPay.assignmentLabel " ...CT = " aTripPay.creditType " ...duty period " it.sequenceNumber " ...reports in SP = " reportsInSP).//fcnShow("===>>> SP = " aSchedulePeriodPay.schedulePeriodName " ... " tripStr " " aTripPay.tripNameAndDate  " ...LBL = " aTripPay.assignmentLabel " ...CT = " aTripPay.creditType " ...duty period " it.sequenceNumber " ...sumLegBaseCredits = " it.sumLegBaseCredits). if (reportsInSP) then{if (it.rigsGreaterThanPremium = false) then{//fcnShow("CB 1 ... handling carry-in trip " aTripPay.tripNameAndDate " DP = " it.sequenceNumber).dutyPeriodContribution = it.sumLegBaseCredits.}else if (it.creditType = ("F" or "P")) then    //// FOR DE7079{//fcnShow("CB 2 ... handling carry-in trip " aTripPay.tripNameAndDate " DP = " it.sequenceNumber).dutyPeriodContribution = it.payDutyPeriod.basePay.}else if (it.creditType = "M") then{//fcnShow("CB 3 ... handling carry-in trip " aTripPay.tripNameAndDate " DP = " it.sequenceNumber).dutyPeriodContribution = math().max(it.sumLegBaseCredits, it.dutyPeriodMinimum).}else{//fcnShow("CB 4 ... handling carry-in trip " aTripPay.tripNameAndDate " DP = " it.sequenceNumber).dutyPeriodContribution = it.sumLegBaseCredits + it.dutyPeriodRig + it.tripExcess.  //// DE7515 added trip excess}contribution += dutyPeriodContribution.}if (it.tripPay.assignmentLabel = ("J" or "U" or "V" or "D" or "E") and          it.thisMonthPay > it.sumLegTotalCredits) then{dutyPeriodContribution = it.thisMonthPay - it.sumLegTotalCredits.contribution += dutyPeriodContribution.}//fcnShow("===>>> SP = " aSchedulePeriodPay.schedulePeriodName " ... " tripStr " " tripName  " ...LBL = " aTripPay.assignmentLabel " ...CT = " aTripPay.creditType " ...duty period " it.sequenceNumber //" contributes " dutyPeriodContribution " ... total trip contribution now = " contribution).}}}else //// HANDLE FORCED TRIPS...{ineligibleDutySum is a real initially 0.0.for each DutyPeriodPay in aTripPay.dutyPeriodPayList as an array of DutyPeriodPay do{reportsInSP is a boolean initially false.reportsInSP = fcnDoesDutyPeriodPayBeginInSchedulePeriodPay(it, aSchedulePeriodPay).if (fcnIsDateTimeWithinReportFilterRange(fcnGetFirstLegOfDutyPeriod(it.payDutyPeriod).scheduledDepartureDateTime, reportDateRangeFilterStart, reportDateRangeFilterEnd) = false or                                                                reportsInSP = false) then{//fcnShow("===>>> subtracting DP " it.sequenceNumber " sumLegBaseCredits of " it.sumLegBaseCredits //" and duty rig of " it.dutyPeriodRig " because DP does not start in SP or is outside report date range").ineligibleDutySum += it.sumLegBaseCredits.ineligibleDutySum += it.dutyPeriodRig.}}contribution = aTripPay.basePay - ineligibleDutySum.contribution = fcnRoundUpAt2DecimalPlaces(contribution).//fcnShow("===>>> SP = " aSchedulePeriodPay.schedulePeriodName " ... " tripStr " " tripName " ...label = " aTripPay.assignmentLabel//" ...credit Type = " aTripPay.creditType " contributes trip base - ineligible duty amount = " aTripPay.basePay  " - " ineligibleDutySum " = " contribution)..}if (aTripPay.tripPayInflight.ronRigTotal <> 0 and aTripPay.assignmentLabel = ("J" or "U" or "V" or "D" or "E")) then{contribution += aTripPay.tripPayInflight.ronRigTotal.fcnShow("===>>> Carry-In... Adding RON-RIG of Duty rig of " aTripPay.tripPayInflight.ronRigTotal " to trip " aTripPay.tripNameAndDate "'s contribution ... value now = " contribution " ...fcnCalculateTripContributionForProductivityPay").}}//fcnShow("===>>> SP = " aSchedulePeriodPay.schedulePeriodName " ... " tripStr " " aTripPay.tripNameAndDate  " ...LBL = " aTripPay.assignmentLabel " ...CT = " aTripPay.creditType  " contributes " contribution).}return fcnRoundUpAt2DecimalPlaces(contribution).
```

## Dependencias

Esta function llama a:

- [fcn](fcn.md)
- [fcnDoAllDutyPeriodsHaveRapAssociations](fcnDoAllDutyPeriodsHaveRapAssociations.md)
- [fcnDoAllDutyPeriodsInSpHaveRapAssociations](fcnDoAllDutyPeriodsInSpHaveRapAssociations.md)
- [fcnDoesDutyPeriodPayBeginInSchedulePeriodPay](fcnDoesDutyPeriodPayBeginInSchedulePeriodPay.md)
- [fcnGetFirstLegOfDutyPeriod](fcnGetFirstLegOfDutyPeriod.md)
- [fcnGetSumOfDutyRig](fcnGetSumOfDutyRig.md)
- [fcnGetSumOfLegBasePayForTrip](fcnGetSumOfLegBasePayForTrip.md)
- [fcnGetSumOfLegPay](fcnGetSumOfLegPay.md)
- [fcnGetSumOfLegPayForTrip](fcnGetSumOfLegPayForTrip.md)
- [fcnIsDateTimeWithinReportFilterRange](fcnIsDateTimeWithinReportFilterRange.md)
- [fcnIsReserveTrip](fcnIsReserveTrip.md)
- [fcnMaxOf3Numbers](fcnMaxOf3Numbers.md)
- [fcnRoundUpAt2DecimalPlaces](fcnRoundUpAt2DecimalPlaces.md)
- `fcnShow()`
- [ping](ping.md)

